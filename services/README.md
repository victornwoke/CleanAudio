# services

Planned backend, audio, media, storage, and export services. Audio enhancement will live behind typed adapter interfaces (native / cloud / development-mock) — see `docs/project-audit.md` and `AGENTS.md` §4. No screen may call a provider SDK directly; it must go through a service.
