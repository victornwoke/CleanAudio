# lib

Planned ownership boundary for centralized Clerk, RevenueCat, OneSignal, Sentry, and PostHog wrappers. As each SDK is added, initialize it exactly once here, never inside a screen, per `AGENTS.md` §20.
