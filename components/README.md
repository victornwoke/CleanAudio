# components

Reusable visual components (buttons, cards, waveform renderer, etc.). No business logic or SDK calls — presentational only. Organize into subfolders (e.g. `audio/`, `auth/`, `common/`, `export/`, `library/`, `paywall/`, `settings/`) as features land, per `AGENTS.md` §5.
