# CleanAudio — Implementation Status

Status values: `not-started` | `in-progress` | `blocked` | `verified`. Updated by each numbered prompt as it completes; do not mark a feature `verified` without genuine checks (see `CLAUDE.md` §22/§23).

## Foundation

| Item | Status | Notes |
|---|---|---|
| Repository audit (`prompts/00`) | verified | `docs/project-audit.md` produced; `tsc`, `expo lint`, `expo-doctor` all pass; no screen changed. |
| Architecture boundaries (`components/`, `constants/`, `features/`, `hooks/`, `lib/`, `services/`, `store/`, `types/`, `docs/`) | verified | Created empty with a one-line README each; populated by later prompts. |
| `.env.example` | verified | Variable names only, matches `AGENTS.md` §18. |
| type-check / lint / test / verify scripts | verified | `npm run typecheck`, `npm run lint`, `npm run test`, `npm run verify`. Node's built-in test runner covers audio routing and prompt-16 backend security contracts. |

## P0 — MVP launch blocking (PRD §14, §37)

| Feature | Status | Notes |
|---|---|---|
| NativeWind / design-system setup (`prompts/01`, `02`) | in-progress | `prompts/02` (design-system tokens + primitives) verified — see notes below. `prompts/01` (NativeWind conversion pass) not yet executed as its own task. |
| Navigation & route shell (`prompts/03`) | verified | Full route tree created under `src/app/` (existing router root). Tab bar is `Library \| History \| Premium \| Settings` matching `04-home-library.png` and the existing `constants/images.ts` tokens — no dedicated Record tab (record/import reached from the Library CTA); user-confirmed decision over AGENTS.md §7/PRD §16's differing tab wording. See notes below. |
| Demo / onboarding / persona selection (`prompts/04`) | in-progress | Splash, demo, and persona screens built and visually verified on-device. See notes below — one genuine blocker (placeholder demo audio) keeps this from `verified`. |
| Authentication — Clerk (`prompts/05`) | in-progress | `@clerk/expo` installed and wired end to end; a real key is now configured and `AuthSheet` is visually confirmed on-device against `03-authentication.png` (both modes). Not yet `verified` — the actual sign-up/sign-in/verify/reset button taps haven't been exercised on-device (no UI-automation tooling available in this sandbox). See notes below. |
| Home / Library (`prompts/06`) | in-progress | Full library screen built and visually verified (web + on-device simulator). See notes below — kept from `verified` by the local-data/backend prompts (`14`/`16`) it's still waiting on. |
| Record & Import (`prompts/07`) | in-progress | Real recording (native `expo-audio` recorder, live level meter/waveform, pause/resume/stop, interruption handling) and import (Photos/Files pickers, validation, recent imports) built and visually verified on-device against `05-import.png`. See notes below — kept from `verified` by the same no-tap-automation limitation recorded in every prior prompt, plus the processing screen (`prompts/09`) it hands off to not existing yet. |
| Preset selection (`prompts/08`) | verified | Real preset selection screen built and visually verified on-device against `cleanaudio-presets.png`; see verification notes below. |
| Processing screen (`prompts/09`) | in-progress | Real trustworthy-progress screen built and verified on-device + web against `07-processing.png`. See notes below — kept from `verified` by the same adapters/persistence dependencies (`prompts/14`/`15`/`18`) every prior in-progress screen is waiting on. |
| Before/After review (`prompts/10`) | in-progress | Real synchronized compare screen built and verified on-device against `08-before-after.png`. See notes below — kept from `verified` by the same no-tap-automation limitation recorded in every prior prompt, plus Enhanced playback being honestly unavailable until `prompts/15` lands a real adapter. |
| Manual fine-tune (`prompts/11`) | in-progress | Real controls screen built and visually/interactively verified against `06-enhancement-controls.png`. See notes below — kept from `verified` by the same no-tap-automation limitation recorded in every prior prompt, plus the enhancement adapter it hands off to not existing yet. |
| Export & success (`prompts/12`) | in-progress | Real export screen built and verified on-device against `09-export.png`. See notes below — kept from `verified` by the same no-tap-automation limitation recorded in every prior prompt, plus the export/enhancement adapters and RevenueCat entitlement it depends on (`prompts/15`/`17`) not existing yet. |
| History & file detail (`prompts/13`) | in-progress | History tab and full immutable file-detail/version UI implemented; persistence, real adapters, and confirmed cloud deletion await prompts 14–16. See notes below. |
| Zustand & local data (`prompts/14`) | verified | Zustand stores, explicit versioned hydration, narrow selectors, and typed local repository boundaries are implemented. The current repository implementation is intentionally in-memory pending an approved SQLite package; see notes below. |
| Audio domain & adapters (`prompts/15`) | verified | Production contracts, capability routing, unavailable native/cloud seams, and an explicitly opt-in bundled-demo-only development adapter are implemented. No real DSP module or backend exists, so user media enhancement remains unavailable rather than faked. See notes below. |
| Backend cloud sync & jobs (`prompts/16`) | in-progress | Secure transport-neutral contracts, mobile client seam, reference backend, signed-transfer recovery, bounded polling, sync retry/tombstones/conflicts, safe notification payloads, and security tests are implemented. A deployed server/storage/queue is still required. See notes below. |
| RevenueCat subscriptions (`prompts/17`) | in-progress | `react-native-purchases`/`react-native-purchases-ui` installed and configured once at the root layout; real offerings, entitlement lifecycle, purchase, restore, and Customer Center are wired end-to-end and confirmed live against a real RevenueCat test project. Not yet `verified` — no tap-automation is available in this sandbox to exercise an actual sandbox purchase/restore tap or the native Customer Center modal. See notes below. |

## P1 / infra (tracked for visibility, not launch-blocking per PRD §14)

| Feature | Status | Notes |
|---|---|---|
| OneSignal notifications (`prompts/18`) | in-progress | `react-native-onesignal`/`onesignal-expo-plugin` installed and configured once at the root layout; real init, external-ID login/logout, approved-tag sync, permission request/status, and notification-click deep-linking are wired end-to-end and confirmed live against the real OneSignal App ID. Not yet `verified` — no real push has been delivered/tapped (needs OneSignal dashboard APNs config + a physical device). See notes below. |
| Sentry monitoring (`prompts/19`) | in-progress | `@sentry/react-native` installed and configured once at module scope in the root layout; privacy scrubbing, the app-root error boundary, Expo Router navigation instrumentation, and a dev-only diagnostic test event are wired end-to-end. Not yet `verified` — no native rebuild has been performed in this pass (a `expo start --dev-client` session was already active; see notes below) and no real event has been observed landing in the Sentry dashboard. See notes below. |
| PostHog analytics (`prompts/20`) | in-progress | `posthog-react-native` installed and configured once at the root layout; anonymous-then-identified identity, safe person properties, the full typed "Core events" taxonomy, screen tracking, and a dev-time forbidden-property guard are wired end-to-end. Not yet `verified` — no real event has been observed landing in the PostHog dashboard and no on-device tap-automation is available in this sandbox. See notes below. |
| Settings / privacy / help (`prompts/21`) | not-started | |
| Testing, accessibility, performance (`prompts/22`) | not-started | No test runner installed; see foundation note above. |
| Production audit & release (`prompts/23`) | not-started | |
| Figma handoff & visual polish (`prompts/24`) | not-started | |

## Known blockers

- **Demo sample audio is a placeholder, not the real "before/after" marketing sample.** The "Hear the difference" screen (`prompts/04`) bundles two synthetically generated clips (`assets/audio/demo-original.mp3`, `assets/audio/demo-enhanced.mp3` — see `assets/audio/README.md`) so the playback UI is genuinely functional, but they are not a real recorded voice sample and not CleanAudio's actual enhancement pipeline (that pipeline doesn't exist yet — `prompts/15`). **Must be replaced with a real produced before/after recording before release**, per `CLAUDE.md` §8 (audio truthfulness).
- See `docs/project-audit.md` §8 for PRD requirements (real-time call enhancement, on-device ML inference, shared C++ DSP core) that will require custom native modules rather than ordinary Expo/React Native JavaScript when their turn comes.
- **The structured project/version repository is currently in-memory, not SQLite or cloud-synced.** Prompt 14 now owns the typed repository contracts and the library writes through that boundary, but no SQLite option is installed or approved in the repository. `features/library/sampleLibraryData.ts` remains the development seed so the existing catalog UI stays populated; its processed entries remain illustrative and are not claims of real enhancement. A later persistence decision can replace `createInMemoryLocalRepositories()` without changing store consumers; cloud sync remains prompt 16.
- **Import from a native OS share extension is not implemented.** `prompts/07`'s Import UI spec lists "import from share extension where supported" as a should-have. A genuine entry in other apps' share sheets requires a native iOS Share Extension target (config plugin + Xcode target) or Android intent-filter beyond ordinary Expo/RN JavaScript, and no approved package for this exists in `AGENTS.md`'s stack. Not built in this pass — flagged rather than faked with a non-functional row.

## `prompts/20-posthog-analytics.md` — verification notes (2026-07-12)

- **Starting state was not a clean slate.** Before this pass began, the working tree already contained an uncommitted, partial PostHog integration (`lib/analytics/posthog.ts`, a modified `lib/analytics/events.ts`, a modified `src/app/_layout.tsx`, and event calls added to several already-verified screens) plus a stray `posthog-setup-report.md` at the repo root and a `.claude/skills/integration-expo/` folder. This was the output of an automated PostHog setup tool, not a `CLAUDE.md`-governed pass — it used non-spec event names (`file_imported`, `preset_confirmed`, `processing_completed`/`processing_failed`/`processing_cancelled`, `user_signed_in`/`user_signed_up`) instead of this prompt's exact required "Core events" list, and was missing two of the four required files (`lib/analytics/properties.ts`, `hooks/useScreenTracking.ts`). Per `CLAUDE.md` §2/§7 ("Do not implement from the user prompt alone... Identify existing working behaviour that must be preserved" and the current numbered prompt outranking ad hoc existing code), this pass reconciled that code against the prompt's exact spec rather than either discarding it or leaving the mismatched names in place. `posthog-setup-report.md` (an unrequested generated doc, forbidden by `CLAUDE.md`'s "Do not create documentation files... unless explicitly requested") was deleted; `.claude/skills/integration-expo/` was left in place as ordinary agent tooling, not application code.
- **Added:** `lib/analytics/properties.ts` (this prompt's required file — `buildSafePersonProperties()` for the Identity section's signup-date/persona/locale/plan/preferred-preset payload, and `findForbiddenPropertyKey()`, a dev-time allowlist guard against filename/project-title/transcript/email/signed-URL/local-path property keys; deliberately has no `posthog-react-native` import so it stays pure/dependency-free and unit-testable under plain `node:test`, mirroring `lib/monitoring/scrubEvent.ts`'s pattern) plus `lib/analytics/properties.test.cjs` (4 new passing tests). `hooks/useScreenTracking.ts` (this prompt's other required file — the screen-tracking effect, extracted out of `src/app/_layout.tsx`'s inline `ScreenTracker` component into its own reusable hook per `AGENTS.md` §5's `hooks/` boundary).
- **Changed `lib/analytics/events.ts`:** the `AnalyticsEvent` union now matches this prompt's "Core events" list by name exactly (`app_opened`, `demo_comparison_used`, `media_import_started`/`media_import_completed`, `recording_completed`, `preset_recommended`, `preset_selected`, `enhancement_started`/`_completed`/`_failed`, `comparison_used`, `fine_tune_opened`, `export_started`, `purchase_cancelled`, `notification_primer_viewed`, `notification_permission_result`, `project_deleted`, and the others that were already correctly named). A few additional safe events from earlier prompts were kept alongside them rather than deleted (`purchase_failed`/`restore_failed` as `*_cancelled`/`*_completed` counterparts, `onboarding_cta_tapped`/`onboarding_completed`, `user_signed_in`/`user_signed_up`, `fine_tune_applied`/`fine_tune_reset`, `review_feedback_submitted`, `processing_notify_opt_in_changed`, `enhancement_cancelled`) — extra safe events aren't forbidden by the prompt, only unsafe properties are, and deleting already-wired, already-typed call sites from verified prompts would be out of this prompt's scope. `track()` now also runs `findForbiddenPropertyKey()` in `__DEV__` and drops (rather than sends) any event whose payload carries a forbidden key, so a future call site can't silently leak PII through this typed boundary — never blocks in production, matching "analytics failure must never block the user."
- **Rewired call sites to the corrected event vocabulary and filled every Core-event gap:** `features/import/useImportScreen.ts` (`file_imported` → `media_import_completed`; added `media_import_started` at the start of both the Photos and Files pickers), `features/presets/usePresetSelectionScreen.ts` (`preset_confirmed` → `preset_selected`; added `preset_recommended` when the recommendation resolves), `features/processing/useProcessingScreen.ts` (`processing_completed`/`_failed`/`_cancelled` → `enhancement_completed`/`_failed`/`_cancelled`; added a fire-once-per-job `enhancement_started`, guarded by the same kind of module-level `Set` the existing terminal-status guard already used, so remounting the same job never double-fires it), `features/record/useRecordScreen.ts` (added `recording_completed` once the finalized project resolves), `features/review/useReviewPlayback.ts` (added `comparison_used` in `selectTrack` — shared by both the Review and Fine-tune screens, since both reuse this hook for the same before/after toggle), `features/fineTune/useFineTuneScreen.ts` (added `fine_tune_opened` on mount), `features/export/useExportScreen.ts` (added `export_started` in `startExport()`), `src/app/paywall.tsx` (a purchase result of `purchase_cancelled` now tracks the dedicated `purchase_cancelled` event instead of always tracking `purchase_failed`), `features/onboarding/useDemoPlayback.ts` (`demo_original_played`/`demo_enhanced_played` → the single `demo_comparison_used` with a `variant` property), `components/notifications/PermissionPrimer.tsx` (added a fire-once `notification_primer_viewed` on mount), `features/notifications/useNotificationPermission.ts` (added `notification_permission_result` once `requestOneSignalPushPermission()` resolves), `features/history/historyCatalog.ts` (added `project_deleted` on every real deletion outcome, not on `"partial"`).
- **Identity (`src/app/_layout.tsx`):** `identitySync.ts`'s existing bare `posthog.identify(clerkUserId)` / `posthog.reset()` (login/logout transition only) is unchanged. Added `AnalyticsIdentityBootstrap`, a reactive component mirroring `NotificationsBootstrap`'s existing OneSignal tag-sync pattern — while signed in, it re-calls `posthog.identify(user.id, buildSafePersonProperties(...))` whenever Clerk's `user.createdAt`, the onboarding store's `personaId`/`defaultPresetId`, or RevenueCat's `isPro` change, so PostHog's person properties (`signup_date`, `persona`, `locale`, `plan`, `preferred_preset`) stay current rather than only being set once at the login instant. Guests are untouched (the effect returns early when `!isSignedIn`), consistent with `AGENTS.md` §12 "Identify with Clerk user ID only after authentication."
- **`app_opened` (`src/app/_layout.tsx`):** added `AnalyticsLifecycleTracker`, firing once on mount (cold start) and again on every background/inactive → active `AppState` transition. PostHog's own `captureAppLifecycleEvents: true` (already set in `lib/analytics/posthog.ts`) separately captures its own default-named lifecycle events (`Application Opened`, etc.) — this is the additional typed, snake_case event this app's analytics vocabulary otherwise uses everywhere.
- **`ScreenTracker` in `src/app/_layout.tsx`** now just calls the new `useScreenTracking()` hook (moved there per this prompt's required-files list) instead of holding the pathname-diffing logic inline. Behaviour is unchanged: `posthog.screen()` fires once per distinct pathname, `captureScreens: false` on `PostHogProvider`'s autocapture avoids a duplicate source of screen events (Expo Router's route groups don't map cleanly onto React Navigation v7's own auto screen capture).
- **Autocapture was checked for a privacy leak and found already safe.** `PostHogProvider`'s `autocapture.propsToCapture` (pre-existing, from before this pass) is set to `["testID"]`, overriding the SDK's own default (`['style', 'testID', 'accessibilityLabel', 'ph-label', 'children']`, read directly from the installed `posthog-react-native@4.55.0`'s `.d.ts`). Since `accessibilityLabel`/`children` are excluded, tapping a Library row or File Detail button cannot autocapture a real project title or filename as an event property — verified by reading the installed SDK's type definitions rather than assuming the default is safe.
- **`lib/analytics/posthog.ts` was reviewed and left as-is:** the client singleton reads only `EXPO_PUBLIC_POSTHOG_KEY`/`EXPO_PUBLIC_POSTHOG_HOST` (both already set in `.env`/named in `.env.example`, matching `AGENTS.md` §18), is `disabled` (a safe no-op) when the key is absent, and does not call `posthog.startSessionRecording()` or configure session-replay masking anywhere — session replay is disabled by omission, matching this prompt's "Session replay: keep disabled until masking is verified."
- **`package.json`'s `test` script** now also compiles `lib/analytics/properties.ts` through the existing pure-logic `tsc` pass and runs `lib/analytics/*.test.cjs` through `node --test`, alongside the existing `services/audio`/`services/api`/`lib/monitoring` suites.
- **Verification performed:** `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npm run test` (24/24 passing, including the 4 new `properties.ts` tests), `npx expo-doctor` (20/20 checks passed). No native module was added — `posthog-react-native` was already installed and linked by the pre-existing state this pass started from, and no `app.json` plugin entry or native rebuild is required for it (confirmed by inspecting the installed package for an `expo.plugins`/`app.plugin.js` entry — none exists, same as RevenueCat's packages in `prompts/17`'s notes).
- **Not verified:** no real event has been observed landing in the PostHog dashboard (`eu.posthog.com`) — this pass changed and re-verified the code but did not independently confirm live delivery, since doing so would require running the dev-client build and watching the dashboard in real time, which this sandbox cannot do. No on-device tap-automation is available in this sandbox (the same limitation recorded in every prior prompt's notes), so the actual button taps that trigger `preset_selected`, `export_started`, `notification_primer_viewed`'s primer render, etc. were code-reviewed and type-checked but not tap-driven end-to-end.
- **Not done in this pass (explicitly out of scope):** a PostHog dashboard/insights setup — a dashboard task, not client code. The pre-existing wizard's report claimed it had already created a dashboard and five insights against a specific project; this pass did not independently confirm which PostHog project `EXPO_PUBLIC_POSTHOG_KEY` in this `.env` currently points at (the value was not inspected, per this session's own credential-handling constraints), so whether that dashboard still corresponds to this app's real event stream is unconfirmed and was neither relied upon nor touched. Mounting a dev-only "send PostHog test event" diagnostic button was also not done — unlike `prompts/19`, this prompt does not name one as a required file/acceptance criterion, so none was speculatively added.

## `prompts/19-sentry-monitoring.md` — verification notes (2026-07-12)

- **Added:** `lib/monitoring/sentry.ts` (the centralized SDK wrapper — `configureSentryOnce()` mirroring `lib/purchases/revenuecat.ts`'s once-only guard, the exported `navigationIntegration`, `setSentryUserId()`, `captureError()`/`addSafeBreadcrumb()` restricted to the typed `SafeMonitoringContext` fields from this prompt's "Safe contexts" list, `startMonitoringSpan()` covering the named "Performance spans", and `captureMonitoringTestEvent()` for the required diagnostic verification path). `lib/monitoring/scrubEvent.ts` (pure, dependency-free privacy scrubbing — no `@sentry/react-native` import, so it type-checks and unit-tests under plain `node:test` the same way `services/audio/enhancementRoutingPolicy.ts` does — plus `lib/monitoring/scrubEvent.test.cjs`, 9 new passing tests covering every category this prompt's "Privacy defaults" list names: auth/cookie headers, signed-URL query strings, local file paths, filenames, transcripts, user email/username/ip, OneSignal/purchase-transaction identifiers, breadcrumb data). `components/common/AppErrorBoundary.tsx` (the actual render-crash safety net — see below for why `Sentry.wrap` alone isn't one).
- **Changed:** `src/app/_layout.tsx` (`configureSentryOnce()` called at module scope before any component renders; `MonitoringNavigationBridge` registers Expo Router's `useNavigationContainerRef()` with Sentry's navigation integration; the whole `<Stack>` wrapped in `<AppErrorBoundary boundary="app_root">`; default export changed to `Sentry.wrap(RootLayout)`). `features/auth/identitySync.ts` (`setSentryUserId(clerkUserId)` / `setSentryUserId(null)` added alongside the existing RevenueCat/OneSignal identity calls). `app.json` (`@sentry/react-native` config plugin added by `npx expo install @sentry/react-native`, then given explicit `organization`/`project` — these are non-secret identifiers, same class as the already-committed RevenueCat/OneSignal app IDs; `authToken` deliberately left unset so the plugin falls back to the `SENTRY_AUTH_TOKEN` env var at build time, verified by reading the installed plugin's own `getSentryProperties()` source). `metro.config.js` (`getSentryExpoConfig` from `@sentry/react-native/metro` replaces the direct `expo/metro-config` call — it already calls Expo's own `getDefaultConfig` internally, so `withNativewind` still composes on top exactly as before, per `CLAUDE.md` §16). `components/dev/DesignSystemShowcase.tsx` (new "Monitoring" section with a test-event button — this prompt's own required diagnostic path). `components/index.ts` (barrel export). `.env`/`.env.example` (`EXPO_PUBLIC_SENTRY_DSN` already present; filled with the provided project DSN). `package.json` `test` script (added `lib/monitoring/scrubEvent.ts` to the existing pure-logic `tsc` compile pass and `lib/monitoring/*.test.cjs` to the `node --test` glob).
- **Installed version (`@sentry/react-native@7.11.0`, via `npx expo install @sentry/react-native`, Expo's own compatibility resolution for SDK 57) predates the `expoRouterIntegration`/`isRunningInExpoGo`/`wrapExpoRouter` API that current top-level Sentry docs show.** Verified directly against the installed package's own `.d.ts` exports (`node_modules/@sentry/react-native/dist/js/index.d.ts`) rather than trusting the docs page, per `CLAUDE.md` §16 ("do not follow stale examples when the installed package uses a different setup" — here it's the reverse: the *docs* were ahead of the *installed version*). Used the still-exported `reactNavigationIntegration()` instead, registered against Expo Router's own `useNavigationContainerRef()` (the documented pre-`expoRouterIntegration` technique, confirmed Expo Router's hook just returns a stable module-level ref regardless of call site) — functionally equivalent navigation instrumentation, correct for this installed version.
- **`Sentry.wrap()` in this installed version does not include an error boundary or fallback UI** — read directly from `node_modules/@sentry/react-native/dist/js/sdk.js`: it only adds a `TouchEventBoundary` and a `Profiler`, no `componentDidCatch`. This is exactly why this prompt's required-files list includes a separate `AppErrorBoundary.tsx` — it's the actual crash boundary, built on `Sentry.ErrorBoundary` (re-exported from `@sentry/react`) with this app's own `ErrorState`/`AppScreen` fallback UI instead of Sentry's default blank/no-UI behavior. A caught render error is treated as unrecoverable for the crashed subtree (`CLAUDE.md` §7 distinguishes recoverable from unrecoverable) — the fallback offers "Try again" (`resetError`) and a safe "Back to Library" exit via `expo-router`'s imperative `router.replace`, both real, working actions.
- **Performance-span wiring is a reusable helper (`startMonitoringSpan`), not yet retrofitted into `services/audio`/`services/api`/`features/export`.** This prompt's own "Required files" list names exactly three files (`lib/monitoring/sentry.ts`, `lib/monitoring/scrubEvent.ts`, `components/common/AppErrorBoundary.tsx`) and none of them are in `services/`/`features/`. Threading `startMonitoringSpan` through every already-completed prompt's call site (upload, job creation/wait, export, import inspection, review load) would mean editing several previously-verified prompts' files well beyond this prompt's own scope (`CLAUDE.md` §20: "remain within its scope; avoid speculative implementation from future prompts"). `navigation_display` timing is automatic via `navigationIntegration`'s `enableTimeToInitialDisplay`; `app_bootstrap` is covered by the SDK's own automatic native App Start tracing (enabled by default once the native module links) rather than hand-rolled timing code. Flagged here rather than silently left unmentioned — a follow-up pass wiring `startMonitoringSpan`/`captureError` into those existing async boundaries would close this out.
- **No native rebuild was performed in this pass.** `npx expo prebuild`/`npx expo run:ios` (the same verification prior prompts 17/18 used) were deliberately skipped because an `expo start --dev-client --clear` process was already running against this same checkout when this task reached its verification stage — regenerating `ios/` or spawning a second Metro instance under it risked disrupting that active session rather than a `CLAUDE.md`-sanctioned scoped change. `npx expo-doctor` (20/20 checks passed) and reading the installed `@sentry/react-native/expo` config plugin's own source confirm the plugin is wired correctly, but the actual Xcode build phase script insertion, `sentry.properties` file generation, and a real device/simulator event landing in the Sentry dashboard are unconfirmed. The dev-only "Send Sentry test event" button (`components/dev/DesignSystemShowcase.tsx`) is ready to close this out once a rebuild is run.
- **Not done in this pass (explicitly out of scope):** `SENTRY_AUTH_TOKEN` EAS/CI secret configuration — this repository has no `eas.json`/EAS project configured yet (confirmed absent), so release-build source-map upload cannot be exercised regardless; a Sentry alert-routing/dashboard setup is a dashboard task, not client code.

## `prompts/18-onesignal-notifications.md` — verification notes (2026-07-12)

- **Added:** `lib/notifications/onesignal.ts` (the centralized SDK wrapper — `getOneSignalAppId()`, `configureOneSignalOnce()` mirroring `lib/purchases/revenuecat.ts`'s pattern, `loginOneSignalUser()`/`logoutOneSignalUser()`, `setOneSignalTags()` restricted to the five approved keys, `getOneSignalPermissionStatus()`/`requestOneSignalPushPermission()`, `addNotificationClickListener()`/`removeNotificationClickListener()`). `features/notifications/useNotificationPermission.ts` (the real OS-permission hook, deliberately independent of the durable "notify me" intent preference). `features/notifications/notificationRouter.ts` (`routeNotificationClick()` — deep-link parsing/navigation). `components/notifications/PermissionPrimer.tsx` (the contextual "Get a notification when your audio is ready." prompt covering not_determined/authorized/provisional/denied/unavailable). `components/notifications/NotificationPreferenceRow.tsx` (a real, self-contained settings-row control — not yet mounted, see below).
- **Changed:** `src/app/_layout.tsx` (added `NotificationsBootstrap`, configuring OneSignal exactly once, attaching the notification-click listener in the same effect so a cold-start launch-by-tap isn't missed, and reactively syncing the five approved tags — plan, persona, preferred_preset, locale, processing_notifications_enabled — from `useEntitlementStatus()`/`useOnboardingStore`/`usePreferencesStore`/`Intl`). `features/auth/identitySync.ts` (uncommented the OneSignal lines — `loginOneSignalUser`/`logoutOneSignalUser` now real, same call-site shape prior prompts already committed to). `src/app/processing/[jobId].tsx` (the existing inline "Notify me when complete" `AppButton` replaced with `<PermissionPrimer optedIn={notifyOptedIn} onAccept={toggleNotifyOptIn} onDark />` — same slot `07-processing.png` already approved for the old button in `prompts/09`'s verification). `app.json` (`onesignal-expo-plugin` added as the **first** plugin in the array, per the plugin's own README requirement, with `mode: "development"`). `.env`/`.env.example` (`EXPO_PUBLIC_ONESIGNAL_APP_ID` filled with the provided App ID `7c6238e0-ce85-4489-a84e-51b4ac1f597d`). `components/index.ts` (barrel exports).
- **SDK versions selected per the OneSignal AI-integration doc's own required process** (`https://onesignal.github.io/sdk-releases/releases.json`, Stable/Current track only, no guessing): `react-native-onesignal@5.2.14` (ReactNative Stable) and `onesignal-expo-plugin@2.7.0` (Expo plugin has no Stable channel published, so its only Current release was used).
- **Deep-link payload reuses the existing, already-tested allowlist instead of a second parser.** `prompts/18`'s own "Payload rules" section gives an *illustrative* JSON shape (`{"route": "processing-result", ...}`); the backend contract built in `prompts/16-backend-cloud-sync-and-jobs.md` (`lib/notifications/jobCompletionPayload.ts#parseJobCompletionPayload`, already covered by `services/api/cloudBackend.test.cjs`) implements the same intent — an opaque-ID-only, closed-field job-completion payload — under the field name `type: "enhancement_job_completed"`. Reusing it (`AGENTS.md` §5: no duplicate architectures) means deep-link ID validation is already unit-tested rather than needing a second, parallel parser for a payload shape nothing in this repo actually emits yet.
- **Notification taps route to File Detail (`/file/[projectId]`), not Review.** Review requires a full `AudioProject` reconstructed from route params that a bare push payload can never supply (`features/audio/parseAudioProjectParams.ts`). File Detail already loads solely from a `projectId` via `useProjectHistory` and renders a safe "not found"/error state when the project doesn't exist or isn't owned by the signed-in user — exactly the "fetch authorized data after navigation" / "safe error if not found or not owned" behaviour this task requires, reached with zero new gating code.
- **No push has actually been delivered or tapped on a real device.** This repository's backend (`prompts/16`) is a reference implementation that is not deployed, so nothing in this codebase can trigger a real OneSignal send yet — the only way to test end-to-end delivery is a manual REST/dashboard test push, which requires OneSignal dashboard APNs credentials (not configured in this pass, out of scope for client code) and a physical device (Apple Push doesn't deliver to the iOS Simulator). The click-routing code path (`routeNotificationClick`) was exercised by code review and type-checking only, not a real tap.
- **`NotificationPreferenceRow` is built and functional but not mounted into `src/app/(tabs)/settings.tsx`.** That screen is an explicit placeholder scoped to `prompts/21-settings-privacy-help.md` ("Full settings content ... is built in prompts/21" — its own comment), and `CLAUDE.md` §20 requires staying within the active prompt's scope. The row is a real, working control (reads/writes the same preference `PermissionPrimer` uses, requests permission itself when turned on with permission not yet determined) ready for prompt 21 to drop in without further work.
- **Native rebuild required and performed.** `npx expo install react-native-onesignal@5.2.14 onesignal-expo-plugin@2.7.0`, then `npx expo prebuild --platform ios` — the plugin correctly wrote the App Group entitlement (`group.com.anonymous.CleanAudio.onesignal`) on both the main target and the generated `OneSignalNotificationServiceExtension`, the `aps-environment: development` entitlement, and added the `OneSignalNotificationServiceExtension` Podfile target (`pod 'OneSignalXCFramework'`). `npx expo run:ios` (iPhone 17 simulator, iOS 26.5) — **Build Succeeded, 0 errors** (5 warnings, all the same pre-existing class of benign privacy-manifest deployment-target mismatches already documented in prompts 12/17's notes, now also covering the new NSE target).
- **Verified live on-device**: device syslog (`xcrun simctl spawn ... log show`) confirms `OneSignal.initialize()` made real HTTPS requests to `https://api.onesignal.com/apps/7c6238e0-ce85-4489-a84e-51b4ac1f597d/ios_params.js` and `/apps/7c6238e0-ce85-4489-a84e-51b4ac1f597d/users` — the exact provided App ID reaching OneSignal's real API, not a fabricated/mocked call. `identifyThirdPartyServices()` → `loginOneSignalUser()` fired a real `OSRequestIdentifyUser with external_id: user_3GLpTtCO4HcMHUocrQ4P8sfNpCH` keyed on the actual signed-in Clerk user (confirming the identity-sync wiring). Deep-linked to the processing screen with a synthetic in-progress job — the screen rendered correctly (ring, stage list, waveform) with no crash; the new `PermissionPrimer` occupies the same action-row slot the old button used, but its own rendered state wasn't screenshotted — RN's dev-mode LogBox banner ("Open debugger to view warnings") covered the bottom of the screen and no tap-automation is available in this sandbox to dismiss it or scroll past it (the same tooling limitation recorded in every prior prompt's notes). The underlying warning's content was not identifiable from the device syslog (LogBox output doesn't route through `os_log`); given `npm run typecheck`/`npm run lint` are both clean, this is most likely a pre-existing/third-party dev-only warning unrelated to this change, but that is not confirmed.
- **Not verified:** an actual permission-request tap (`PermissionPrimer`'s primary action), the resulting native iOS permission dialog, `NotificationPreferenceRow`'s switch, and `openSettings()`'s deep link to the OS Settings app were code-reviewed and type-checked but not tap-driven, for the same no-tap-automation reason as every prior prompt. A single transient `OSPropertyOperationExecutor update properties request failed` was observed in the device log during the very first tag-sync call (immediately after cold-start, before the SDK had finished creating its user record) — the SDK's own internal retry queue, not application code; it did not recur on subsequent activity and the app did not crash, but genuine push delivery through this path is unconfirmed.
- **Not done in this pass (explicitly out of scope):** OneSignal dashboard configuration (APNs key/certificate upload, FCM credentials) — a dashboard task, not a client-code task; PostHog dispatch for `processing_notify_opt_in_changed` (still the dev-console `track()` stub until `prompts/20`); Sentry scrubbing for notification-related errors (`prompts/19` not installed yet); mounting `NotificationPreferenceRow` into a real Settings screen (`prompts/21`).

## `prompts/17-revenuecat-subscriptions.md` — verification notes (2026-07-12)

- **Added:** `types/subscription.ts` (`SubscriptionPlanId` = "monthly"/"yearly"/"lifetime", `SubscriptionErrorCode`, `SubscriptionPackageInfo`, `SubscriptionLifecycle`, `SubscriptionEntitlementState` — see the single-entitlement note below). `lib/purchases/revenuecat.ts` (the centralized SDK wrapper — `PRO_ENTITLEMENT_ID` constant, `getRevenueCatApiKey()`/`configureRevenueCatOnce()` mirroring `lib/auth/clerk.ts`'s pattern, `loginRevenueCatUser()`/`logoutRevenueCatUser()`, `resolvePlanId()`/`extractSubscriptionPackages()`/`findPackageForPlan()`, `deriveEntitlementState()` (the real `CustomerInfo` → lifecycle classifier), `mapPurchasesError()`, `presentCustomerCenter()`). `features/subscriptions/useSubscription.ts` (the real hook: offerings + customer-info fetch, a live `addCustomerInfoUpdateListener` subscription, typed `purchase()`/`restore()`/`refreshOfferings()`). `features/subscriptions/subscriptionErrorMessages.ts`, `features/subscriptions/planSavings.ts` (real yearly-savings-% computed from actual store prices, never a hardcoded badge), `features/subscriptions/purchaseCta.ts` (CTA copy derived only from a real free-trial intro price, never an invented trial length). `store/useSubscriptionUiStore.ts` (ephemeral paywall plan-selection UI state only — no entitlement authority). `components/paywall/{PlanCard,FeatureComparison,PurchaseLegalText}.tsx`.
- **Changed:** `features/subscriptions/useEntitlementStatus.ts` (now a thin wrapper over `useSubscription()`, same `{isPro, source}` shape so the export screen's existing Pro-gated quality tiers/watermark switch needed no changes). `features/auth/identitySync.ts` (`identifyThirdPartyServices`/`detachThirdPartyServices` now call real `loginRevenueCatUser`/`logoutRevenueCatUser` instead of a commented placeholder). `src/app/_layout.tsx` (added `PurchasesBootstrap`, configuring RevenueCat exactly once, independent of Clerk auth state since RevenueCat supports an anonymous pre-login identity; `paywall`/`subscription` routes switched to `headerShown: false` since both screens render their own header, the same double-header class of bug prior prompts caught for other full-screen routes). `src/app/paywall.tsx` and `src/app/subscription.tsx` (real screens replacing the `prompts/03` placeholders). `src/app/(tabs)/premium.tsx` (now reflects real `isPro` state instead of always inviting an upgrade). `constants/images.ts` (`premiumHero`, `radioSelected`, `radioUnselected`, `discountBadge`). `lib/analytics/events.ts` (`paywall_viewed`, `purchase_started`, `purchase_completed`, `purchase_failed`, `restore_completed`, `restore_failed` — safe properties only: plan id, error code, boolean outcome, never payment details). `components/index.ts` (barrel exports). `.env`/`.env.example` (RevenueCat iOS/Android keys filled with the provided test key `test_ydSnCTdFNEaUlTwloRRuzniPzqf`; added `EXPO_PUBLIC_TERMS_URL`/`EXPO_PUBLIC_PRIVACY_URL` names for the paywall's legal links).
- **Single entitlement, not the prompt's Free/Pro/Studio example.** The prompt's own "Suggested entitlements"/"Feature matrix" sections are explicitly illustrative ("Example only... Do not assume product IDs"). The user's explicit integration instructions for this task name one entitlement ("Clean Audio Pro") and three packages ("Lifetime (lifetime)", "Yearly (yearly)", "Monthly (monthly)") — a later, more specific instruction that outranks the prompt's example per `CLAUDE.md` §2/`AGENTS.md` §2. This also matches the shape `features/subscriptions/useEntitlementStatus.ts` already committed to (`isPro: boolean`), so no existing call site changed shape.
- **RevenueCat's own hosted Paywall template (`react-native-purchases-ui`'s `presentPaywall`) was deliberately not used for the primary paywall surface.** `react-native-purchases-ui` is installed and used for **Customer Center** (`presentCustomerCenter()`, wired into `src/app/subscription.tsx`'s "Manage Subscription" button), but the paywall itself is a bespoke screen built to match `11-paywall.png` pixel-for-pixel using this app's own design tokens/components and real offerings data fetched via `Purchases.getOfferings()`. `CLAUDE.md` §2/§6 require the local PNG as the required visual source; RevenueCat's hosted paywall template would render RevenueCat-dashboard-authored content, not this approved design, without separately configuring a matching remote template (out of scope here, and not requested with that level of specificity). Flagged here as a design-fidelity-over-tool-suggestion call, consistent with this repository's practice of flagging PNG-vs-instruction interpretations rather than silently picking one.
- **"Priority processing" and "Advanced AI models" (two of `11-paywall.png`'s five feature bullets) describe Pro capabilities this build cannot yet deliver** — no priority job queue or multi-model routing exists anywhere in this codebase (`docs/project-audit.md` §8). Reproduced verbatim from the PNG anyway, on the same "gate behind a real entitlement, backend catches up" posture the export screen's Pro-gated quality tiers already use (prompt 12's own notes flag the same class of gap for WAV 24-bit/MP3 320kbps). This is a product-copy call worth a second look before release, not a fabricated claim that a specific action already happened (`CLAUDE.md` §8 is about not reporting that enhancement occurred when it didn't — it does not obviously forbid describing a paid plan's intended feature set). Flagged here rather than silently trimmed or silently shipped.
- **The PNG's crown illustration has no equivalent in this app's icon set and no image-generation tool exists in this build.** `constants/images.ts#premiumHero` substitutes Ionicons' "trophy" glyph (the closest semantic match to "achievement/premium" already available, alongside the existing "star" used for the Premium tab). Flagged rather than silently approximated, per `AGENTS.md` §6's require-exact-match rule.
- **Terms/Privacy links in `PurchaseLegalText` render only when `EXPO_PUBLIC_TERMS_URL`/`EXPO_PUBLIC_PRIVACY_URL` are configured** (both empty in this pass — no real Terms of Use/Privacy Policy destination exists anywhere in this repository yet; `prompts/21-settings-privacy-help.md` is not-started). This assistant does not fabricate a URL for the user (a standing rule for this session), and shipping a tappable link with no real destination would be a "dead button" (`AGENTS.md` §20). "Restore Purchases" is always real and always rendered. **Apple requires a working Terms/Privacy link for auto-renewing subscriptions (App Store guideline 3.1.2) — both env vars must be set to real pages before this paywall ships.**
- **RevenueCat's client entitlement classifier cannot fully distinguish "grace period" from "billing issue"** (`AGENTS.md` §9 lists them as two states) — `PurchasesEntitlementInfo` only exposes `billingIssueDetectedAt` on an entitlement that is still `isActive`; telling "still in a Play/App Store billing retry window" apart from "in an account hold" needs Google's Real-Time Developer Notifications server-side, which this repository's backend (`prompts/16`, itself not deployed) does not implement. `types/subscription.ts#SubscriptionLifecycle` deliberately has one `"billing_issue"` value (not an unreachable second `"grace_period"` value) whose UI copy in `src/app/subscription.tsx` covers the grace-period case honestly ("you still have access — update your payment method").
- **Native rebuild required and performed.** `npx expo install react-native-purchases react-native-purchases-ui` (10.4.2, the latest version, compatible with the installed RN 0.86/Expo ~57 per its `>= 0.73.0` peer range). Neither package needs an `app.json` config plugin (confirmed by inspecting their installed `package.json`s — no `expo.plugins`/`app.plugin.js` entry, unlike Clerk). Ran `npx expo prebuild --platform ios` (CocoaPods installed cleanly, RevenueCat/PurchasesHybridCommonUI pods resolved) then `npx expo run:ios` — **Build Succeeded, 0 errors** (3 pre-existing unrelated privacy-manifest deployment-target warnings already present before this change, same class already documented in prompt 12's notes).
- **Verified on-device (iPhone 17 simulator, iOS 26.5, dev-client build) against a real RevenueCat test project** using the provided `test_ydSnCTdFNEaUlTwloRRuzniPzqf` key: deep-linked to `cleanaudio://paywall` — the screen renders the hero icon, all five feature rows, and three real plan cards (Monthly $9.99/month, Yearly $79.99/year with a genuinely computed "33% off" badge — `1 - 79.99/(9.99×12)`, not a hardcoded "50% off" like the PNG's static mock — and Lifetime $99.99 one-time), matching `11-paywall.png`'s layout/colors/radii/spacing. Metro's debug log confirms real network activity, not fabricated state: `GetWebBillingProductsOperation`/`GetOfferingsOperation` against `api.revenuecat.com` for products `["lifetime","monthly","yearly"]`, a real `LogInOperation` keyed on the actual signed-in Clerk user id (confirming `identitySync.ts`'s wiring), and the `paywall_viewed` analytics event firing. Deep-linked to `cleanaudio://subscription` — correctly rendered the honest "Free Plan" state (this RevenueCat test user genuinely has no active "Clean Audio Pro" entitlement) with a working "Upgrade to Pro" button, not a fabricated Pro state.
- **Not verified:** no `idb`/accessibility-automation tooling is available in this sandbox (the same limitation recorded in every prior prompt's notes) — so an actual purchase tap (sandbox StoreKit purchase sheet), Restore Purchases tap, the native Customer Center modal (`presentCustomerCenter()`), and the paywall's plan-card selection/CTA-label switching were code-reviewed, type-checked, and confirmed reachable via deep link, but not tap-driven end-to-end. `react-native-purchases`/`react-native-purchases-ui` are native-only packages with no web shim, so this screen's interactions cannot be exercised via the web+Playwright fallback prior prompts used for non-native screens either — a real native tap-automation session is required to close this out to `verified`.
- **Not done in this pass (explicitly out of scope):** RevenueCat dashboard webhook configuration and server-side entitlement reconciliation for premium backend jobs (`CLAUDE.md` §15/this prompt's own "Backend" section) — no deployed backend exists yet (`prompts/16`'s own honest limitation); a real Terms of Use/Privacy Policy destination (`prompts/21`); Sentry error scrubbing for purchase failures (Sentry isn't installed until `prompts/19`); PostHog dispatch for the new analytics events (still the dev-console `track()` stub until `prompts/20`, same as every other event in `lib/analytics/events.ts`).

## `prompts/16-backend-cloud-sync-and-jobs.md` — verification notes (2026-07-11)

- Added strict cloud types and a transport-neutral backend contract for all required upload, enhancement-job, project, export, account-deletion, and RevenueCat-webhook domains. `BACKEND_ROUTES` records the required REST mapping without inventing a server framework that is absent from this repository.
- Added a secure executable reference backend: Clerk JWT verification dependency, owner checks with non-enumerating `not_found` responses, metadata validation (content type/size/checksum/duration), five-minute signed uploads, cloud-entitlement checks, job rate limiting, atomic in-process idempotency, optimistic project revisions, deletion tombstones, and idempotent account deletion.
- Added the mobile client seam, one-time expired-signed-URL refresh, bounded exponential polling, retry queue/backoff, explicit server-wins metadata conflict handling that preserves local media, and strict OneSignal completion payload parsing with opaque IDs only.
- The existing account deletion handoff now calls an injected authenticated backend and still fails honestly when none is configured. No SDK, secret, native module, or UI was added; no native rebuild is required. Prompt 16 names no visual references.
- Runtime tests cover cross-user denial across read/update/delete/upload/job creation, concurrent duplicate job idempotency, expired URL recovery, and rejection of private notification fields. `npm test` passes all audio and backend tests.
- Honest limitation: this Expo repository contains no Go/Python/server framework, database, object storage, queue, worker, or deployment configuration. The reference backend is deliberately in-memory and is not mounted as a production HTTP server. Production must implement the same interfaces with atomic database uniqueness constraints, a Clerk server verifier, RevenueCat webhook secret storage, durable entitlement state, rate-limit storage, short-lived object-storage signing, queue workers, and OneSignal delivery.

## `prompts/15-audio-domain-and-adapters.md` — verification notes (2026-07-11)

- Added the production audio domain contract, including assets/versions, inspection, enhancement requests/jobs/progress, loudness, export, typed errors, checksums, idempotency, provenance, and capability data.
- Added recorder, inspector/extraction, waveform, enhancement, playback, export, and service-factory boundaries. Heavy media work remains behind these interfaces.
- Added native and cloud adapter seams that truthfully report unavailable until a real Expo native module and prompt 16 backend exist. Production cannot opt into the development mock.
- The development adapter is explicit and accepts only `bundled-demo` assets; it cannot copy or label user media as enhanced. Adapter provenance is required on every enhancement job/version.
- Routing uses connectivity, duration, device support, entitlement, selected quality, local-model availability, cloud availability, and explicit upload consent. It makes no performance promises.
- No visual references are named in prompt 15 and no UI was changed. These additions are TypeScript-only and require no native rebuild.
- Verified with `npm run typecheck`, `npm run lint`, and the routing-policy tests through `npm run test`.

## `prompts/14-zustand-and-local-data.md` — verification notes (2026-07-11)

- Added Zustand stores for app continuation, onboarding, projects, jobs, export drafts, and non-sensitive preferences. Persisted schemas use explicit version `1`, migration defaults, hydration flags, and narrow selectors; destructive resets are inert outside `NODE_ENV=test`.
- Added repository contracts for projects, versions, lightweight job/export references, media files, and the sync queue. The in-memory implementation is injectable for tests and owns media cleanup; store setters never delete files directly.
- Moved onboarding completion/persona/default preset/demo-heard and contextual job-notification intent behind the versioned stores. The Library now loads and mutates through the project repository/store instead of component-local fixture state.
- Persisted data deliberately excludes raw media, waveform arrays, auth tokens, signed URLs, transcripts, cloud records, and entitlement authority. Media records contain only owned local URIs and metadata behind the media repository.
- No task-specific visual references were listed in prompt 14 and no UI was changed. Zustand is JavaScript-only, so no native rebuild is required.
- Verified: `npm run typecheck` and `npm run lint` pass. `npm run test` remains the repository's intentional failing placeholder because prompt 22 owns test-runner installation; repository behavior is type-checked but not runtime unit-tested in this pass.

## `prompts/13-history-and-file-detail.md` — verification notes (2026-07-11)

- Implemented History to match `10-history.png`: title/search chrome, All/Audio/Video pills, lavender media thumbnails, compact date/duration metadata, and accessible navigation.
- Replaced the file-detail placeholder with waveform, safe rename, required metadata/provenance, immutable original/enhancement/export history, compare/adjust/export/duplicate-settings actions, and explicit deletion scopes.
- Added typed immutable history models and a shared catalog boundary. Failed/cancelled attempts remain visible and never become successful versions; exports reference their source enhancement so re-export does not imply reprocessing.
- Cloud deletion is deliberately honest: until prompts 14–16 provide persistence/sync/backend ownership, the catalog reports partial deletion instead of claiming the remote copy was deleted. Existing development fixtures remain clearly labelled as development samples.
- Verified: `npm run typecheck` and `npm run lint` pass. `npm run test` remains the repository's intentional failing placeholder because no test runner is installed (tracked for prompt 22). No dependency/native-module changes were made, so no native rebuild is required.
- Runtime visual verification was attempted with `npm run web -- --port 8082`, but Metro did not reach ready state in the sandbox's offline mode. The implementation was compared directly with the original-resolution PNG; device interaction remains to be exercised.

## `prompts/12-export-and-success.md` — verification notes (2026-07-11)

- **Added:** `types/export.ts` (`ExportFormat`, `ExportQualityId`/`ExportQualityOption`, `ExportSettings`, `ExportErrorCode`, `ExportJobStatus`/`ExportJobSnapshot`, `ExportResult`, `ExportRequest`, `ExportAdapter` — the typed export boundary `AGENTS.md` §4 requires). `features/export/exportQualityOptions.ts` (the MP3/WAV quality catalog — the real Free/Pro bitrate and bit-depth values `PRD.md` §22/§24 specifies, not invented numbers). `features/export/estimateExportSize.ts` (a genuine deterministic size calculation from real duration + the selected quality's real bitrate/bit-depth, `null` when duration isn't known — never a fabricated estimate, `CLAUDE.md` §8). `features/export/exportErrorMessages.ts` (user-safe message mapping, mirrors `features/processing/processingErrorMessages.ts`). `features/export/useExportScreen.ts` (screen-level composition: settings state, entitlement-gated quality/watermark selection, job lifecycle, success/failure/cancel handling). `features/subscriptions/useEntitlementStatus.ts` (typed client-entitlement seam — always resolves free-tier since RevenueCat isn't installed yet, `prompts/17-revenuecat-subscriptions.md` is not-started; mirrors `features/presets/presetRecommendation.ts`'s always-honest classifier-stub pattern). `services/audio/exportAdapter.ts` (`developmentMockExportAdapter` — the only `ExportAdapter` implementation today, explicitly labelled prototype; see honesty note below). `components/export/FormatOptionChip.tsx` (selectable pill for the MP3/WAV row). Extended `lib/analytics/events.ts` with `export_completed`/`export_failed` (safe properties only — format, error code). Extended `constants/images.ts` with `exportSaveToFiles`/`exportShare` tokens. Exported `loudnessTargetForLufs()` from `features/fineTune/fineTuneDefaults.ts` (was module-private) so the export screen's default loudness target reuses the exact same preset→LUFS mapping fine-tune already uses, rather than a second copy (`AGENTS.md` §5).
- **Changed:** `src/app/export/[projectId].tsx` (real screen replacing the `prompts/03` placeholder). `features/audio/audioProjectNavigation.ts` — added `goToExport()`, and **fixed a real bug found while wiring this screen**: `features/review/useReviewScreen.ts`'s `goExport()` previously called `router.push({ pathname: "/export/[projectId]", params: { projectId: project.id } })` — the same bare-id hand-off mistake already caught and fixed once for every other screen transition in this app (Processing→Review, Review→Fine-Tune — see prior prompts' notes below). Since the export screen reconstructs its `AudioProject` via the shared `parseAudioProjectParams()`, a bare `projectId` would have left every real "Export" tap landing on the "Project not found" error. Fixed by routing `goExport()` through the new `goToExport()` helper (same `getAudioProjectRouteParams()` helper every other hand-off uses). `components/index.ts` (barrel export).
- **Installed `expo-sharing`** (`npx expo install expo-sharing`) — the approved Expo module for the native share sheet (`09-export.png`'s "Share To" row / the required "native share sheet" UI). Not previously installed (confirmed via `docs/project-audit.md`'s dependency list). **This required a fresh iOS development-client rebuild** (`npx expo run:ios`) — the previously-running dev client crashed with `Cannot find native module 'ExpoSharing'` on first deep-link test, confirming the native-module gap before any workaround was attempted, per `CLAUDE.md` §16. The rebuild succeeded (0 errors, 3 pre-existing unrelated Pod privacy-manifest warnings) and was verified live on-device afterward. **Anyone else pulling this branch (or CI) needs to run `npx expo run:ios` / `npx expo run:android` again** before the export screen's Share/Save-to-Files actions will work — a plain `expo start` reload of the existing binary is not enough.
- **No real export or enhancement adapter exists yet** (`prompts/15-audio-domain-and-adapters.md` is not-started), so `getEnhancedAudioResult()` (reused verbatim from `prompts/10`, unchanged) always resolves `null` and the export screen honestly renders its `unavailableReason` empty state ("Export isn't ready yet") instead of the configuration form — the same gate the Review screen's own Export button already applies, so in the real app today Export is unreachable past that point, exactly like Review's Enhanced tab. This was verified live: deep-linking straight to `/export/[projectId]` with a real recorded file renders the empty state, not a fabricated form.
- **`developmentMockExportAdapter` performs a genuine, working local file copy — never a fabricated timer that reports success without touching disk — but deliberately does not perform real audio transcoding**, since no encoder exists anywhere in this codebase (`AGENTS.md` §4's native/cloud adapter boundary — same class of gap as on-device DNN inference, `docs/project-audit.md` §8). Concretely: if the (currently always-absent) enhanced source's real file extension already matches the user's selected export format, the mock copies it via `expo-file-system`'s `File.copy()` into a new app-owned `exports/` file and reports genuine size/URI. If the extension doesn't match, it honestly fails with `unsupported_format` ("Converting to that format isn't available in this build yet") rather than mislabeling a renamed copy as a real conversion (`CLAUDE.md` §8). Cancellation is real but best-effort (checked before the copy settles, matching the same pattern `services/media/mediaImportService.ts#copyIntoSandbox` already uses for import cancellation) and cleans up any partially-written file.
- **"Remove Watermark" and Pro-gated quality tiers (MP3 192/320kbps, WAV 24-bit) are wired to `useEntitlementStatus()`, which always reports free-tier today** (RevenueCat not installed, `prompts/17` not-started) — tapping either one navigates to `/paywall` (still a placeholder screen itself, `prompts/17`'s own scope) rather than silently no-opping or fabricating a granted entitlement (`CLAUDE.md` §10). Because `/paywall` is presented as a modal (`src/app/_layout.tsx`), the underlying export screen is never unmounted, so dismissing it genuinely returns to the exact same in-progress export configuration — satisfying this prompt's "If purchase succeeds, return to this exact export configuration" without needing extra return-URL plumbing, though no real purchase can complete yet to exercise the success half of that path.
- **"Save to Files" and the success screen's "Share" both invoke the same real `expo-sharing` share sheet** (`Sharing.shareAsync`) rather than two different mechanisms — this is an honest simplification flagged per the same practice as prior prompts' PNG-vs-reality calls: Expo's sharing API has no way to pre-target a specific destination app, so `09-export.png`'s literal "Save to Files" button and its separate "Share To" row of app-specific icons (TikTok/Instagram/WhatsApp/More) were **not** built as distinct fake-targeted buttons — that would imply direct-to-app sharing this API can't actually do. "Save to Camera Roll" was also **not built**: it only applies to video-container exports per this prompt's own scope, and video audio extraction doesn't exist yet (`needsAudioExtraction` is never resolved to `false` for a video project — `prompts/15`), so the state is genuinely unreachable today; omitted rather than shipped as a dead button (`AGENTS.md` §20).
- **Format scope is MP3/WAV only, not the PNG's four-chip MP3/WAV/M4A/FLAC row** — this prompt's own "Export options" text says "MP3 and WAV for MVP" explicitly, which outranks the local PNG per `CLAUDE.md` §2/`AGENTS.md` §2. Flagged here per the same practice as prior prompts' PNG-vs-prompt calls.
- **Verified:** `npx tsc --noEmit` (0 errors), `npx expo lint` (0 errors/warnings), `npx expo-doctor` (20/20). Installing `expo-sharing` required a full native rebuild (`npx expo run:ios --device <sim>`, see above) — confirmed `Build Succeeded` with 0 errors. On-device via the rebuilt dev-client session (iPhone 17, iOS 26.5): deep-linked to `cleanaudio://export/<id>` with a real recorded file's params — confirmed the honest "Export isn't ready yet" empty state renders (not a fabricated form), matching the real, always-unavailable-today `getEnhancedAudioResult()` gate. Then, using the same temporary-force technique documented in `prompts/09`'s notes (a manual override in `features/review/enhancedAudioResult.ts`, screenshotted, then fully reverted and confirmed via `git status`/`git diff` that no trace remained, and the app re-verified back to the honest empty state afterward), confirmed the full configuration form renders correctly against `09-export.png`: checkmark ring, "Audio Enhanced!" header/subtitle, project summary card, Remove Watermark row with Pro badge, MP3/WAV format chips (MP3 selected, indigo border+tint), and the Export Quality row (Standard · 128 kbps · good for voice) — all match the PNG's layout, spacing, and colors.
- **Not verified:** no `idb`/accessibility-automation tooling or browser/Playwright tool is available in this session (same limitation recorded in every prior prompt's notes, here without even the web-build fallback some earlier prompts had) — so the actual finger-tap interactions (format chip selection, the quality bottom sheet, the loudness segmented control, the Remove Watermark switch, Save to Files/Cancel, the paywall hand-off, and the success/failure/cancelled screens) were reviewed in code and exercised structurally via the static deep-linked render, but not tap-driven. The real file-copy export path, the real `Sharing.shareAsync` share-sheet invocation, and cancellation mid-copy were code-reviewed and type-checked but not exercised end-to-end, since reaching them requires a genuine enhanced source this build honestly doesn't have yet (see above) — the same class of not-yet-exercisable branch `prompts/10`'s notes recorded for its own two-real-source playback path.
- **Not done in this pass (explicitly out of scope):** the real export/transcoding adapter and real enhancement adapter these settings will eventually configure (`prompts/15-audio-domain-and-adapters.md`); real RevenueCat entitlement checks and the real paywall UI (`prompts/17-revenuecat-subscriptions.md` — `useEntitlementStatus()` is a typed, always-free seam ready for it); Save to Camera Roll for video exports (genuinely unreachable today, see above); direct per-app share targeting (not possible with `expo-sharing`); server-side entitlement verification for premium export (`CLAUDE.md` §10 — a backend concern, `prompts/16`).

## `prompts/11-manual-fine-tune.md` — verification notes (2026-07-11)

- **Added:** `types/fineTune.ts` (`LoudnessTargetId`, `LoudnessTargetOption`, `FineTuneControlId`, `FineTuneSettings` — the typed intent model FR-8 requires; these are values a future enhancement adapter will consume, not DSP the mock performs today). `features/fineTune/fineTuneDefaults.ts` (`LOUDNESS_TARGET_OPTIONS` — the actual `PRD.md` §23 platform values, -16/-14/-23 LUFS, not invented numbers; `getAutoFineTuneSettings()` — the app's fixed Auto Enhance baseline, the literal 80/85/75/70 values shown in `06-enhancement-controls.png`, explicitly documented as a fixed default rather than a per-recording AI computation since no classifier exists yet, `CLAUDE.md` §8; `areFineTuneSettingsEqual()`). `features/fineTune/useFineTuneScreen.ts` (settings state, the Auto/manual toggle behaviour, `isDirty` tracking, the "leaving with unapplied changes" guard via `navigation.addListener("beforeRemove", …)`, and `apply()`). `components/common/Slider.tsx` (accessible drag slider — `PanResponder`-based, the same technique `components/review/CompareTimeline.tsx` already uses since no `GestureHandlerRootView` wraps the app; exposes `accessibilityRole="adjustable"` with increment/decrement actions per this prompt's own rule 42). `components/common/AppSwitch.tsx` (indigo pill toggle matching the PNG's "AI Enhancement" switch — a small custom `Pressable` + Reanimated component, consistent with the design system's existing custom-controls approach rather than the platform-varying native `Switch`, reduced-motion aware like `SegmentedControl`).
- **Changed:** `src/app/fine-tune/[projectId].tsx` (real screen replacing the `prompts/03` placeholder). `src/app/_layout.tsx` (`fine-tune/[projectId]` switched to `headerShown: false` — this screen renders its own header matching `06-enhancement-controls.png`, the same double-header class of bug caught and fixed for every other full-screen route in `prompts/07`–`10`, avoided here before it ever reached a screenshot). `features/audio/audioProjectNavigation.ts` (added `goToFineTune()`, mirroring `goToReview()`'s full-param forwarding). `features/review/useReviewScreen.ts` (`goAdjust()` now calls `goToFineTune(project)`). `lib/analytics/events.ts` (added `fine_tune_applied`/`fine_tune_reset`, safe properties only). `components/index.ts` (barrel exports).
- **A real bug was caught and fixed while wiring the hand-off from Review, not just visual comparison — `goAdjust()` only forwarded `projectId`.** `useReviewScreen.ts`'s `goAdjust()` previously called `router.push({ pathname: "/fine-tune/[projectId]", params: { projectId: project.id } })` — the same bare-id mistake `prompts/10`'s own notes already document once for Processing → Review. Since the fine-tune screen reconstructs its `AudioProject` via the shared `parseAudioProjectParams()` (same as Review/Processing), a bare `projectId` would have left every real "Adjust" tap landing on the "Project not found" error. Fixed by adding `goToFineTune()` to `audioProjectNavigation.ts` (same `getAudioProjectRouteParams()` helper every other hand-off uses) and confirmed via a Playwright run that the resulting URL carries the full param set.
- **A second, pre-existing gap was found and documented, not fixed (out of scope for this prompt): `project.presetId` never survives the shared `AudioProject` reconstruction.** `parseAudioProjectParams.ts`'s `AudioProjectRouteParams` (shared by Processing, Review, and now Fine-Tune) has no `presetId` field at all — only `usePresetSelectionScreen.ts`'s own screen-local param parsing captures it, and only for the one hop into Processing. So `getAutoFineTuneSettings()` and `apply()`'s hand-off to Processing both honestly fall back to the `"podcast"` default preset (the same fallback already used by `features/onboarding/personas.ts`) rather than the recording's actual chosen preset. Flagged here rather than silently accepted, per the same practice as prior prompts' PNG-vs-prose calls — fixing it would mean widening the shared param contract used by three existing screens, which is broader than this prompt's own scope.
- **"Loudness Target" was added even though `06-enhancement-controls.png` doesn't show it, because the current numbered prompt outranks the local PNG per `CLAUDE.md` §2 / `AGENTS.md` §2** (`prompts/11-manual-fine-tune.md`'s own text: "The PRD MVP requires at minimum noise intensity and loudness target"). Implemented as a 3-option `SegmentedControl` (Podcast/Streaming/Broadcast) with the LUFS value shown only as a secondary caption — never the primary label — per this prompt's own rule 52 ("Do not expose raw DSP jargon as the primary label").
- **"AI Enhancement" toggle drives a real Auto/manual mode, not a decorative switch.** The PNG's own screenshot shows the toggle ON with slider values that exactly equal `getAutoFineTuneSettings()`'s baseline (80/85/75/70) — confirmed this reading is correct once built. ON: the four sliders track the Auto baseline and are disabled (dimmed, `accessibilityState.disabled`); OFF: they unlock at whatever value they were already showing, so nothing jumps. Loudness Target is always independently editable (a mastering/publishing choice, not part of the AI/manual split) — this interpretation isn't dictated by the PNG or prompt text explicitly and is flagged here for review, same practice as prior prompts' UI-interpretation calls.
- **No enhancement adapter exists yet (`prompts/15-audio-domain-and-adapters.md` is not-started), so "Apply" is an honest hand-off, not a fabricated instant re-render (`CLAUDE.md` §8).** `apply()` calls the same `goToProcessing()` hand-off `usePresetSelectionScreen.ts#confirm()` already uses — a genuine new job/route transition (the original recording is never touched; a new job id begins), not a claim that the sliders' values changed the audio. The "Preview" Original/Enhanced segmented control reuses `useReviewPlayback()` verbatim (not a duplicate playback engine, `AGENTS.md` §5) — Original is real, immutable-source playback; Enhanced is honestly reported unavailable with the same caption convention as the Review screen, never a fabricated preview. Because no adapter or cloud job exists to call, "Debounce preview requests" and "Preview only the selected region" (this prompt's own Interaction rules) have nothing to attach to yet — slider drags only ever mutate local component state, so "never fire a cloud job on every slider pixel movement" is trivially and honestly satisfied; these seams are ready for `prompts/15` rather than built as unused/dead infrastructure now.
- **"Leaving with unapplied changes prompts clearly" is implemented via `navigation.addListener("beforeRemove", …)`** (the standard React Navigation "prevent going back" recipe, which Expo Router's `Stack` supports directly) rather than a custom back-button override, so it catches every exit path — header back, hardware/gesture back, and programmatic navigation — not just one button. An `isApplyingRef` flag set synchronously in `apply()` before calling `goToProcessing()` ensures a genuine Apply is never itself blocked by its own dirty-state. Verified via Playwright: making the screen dirty then tapping the header back button left the URL unchanged (the navigation was genuinely intercepted); tapping "Enhance Audio" while dirty navigated straight to `/processing/[jobId]` without being blocked. **The confirmation dialog's copy could not be visually verified on web** — `react-native-web`'s `Alert.alert` is an intentional no-op stub (`static alert() {}`, confirmed by reading its source), the same pre-existing platform gap this codebase already relies on for `record.tsx`'s discard-confirmation and `library.tsx`'s delete-confirmation. The `beforeRemove` interception itself (the part that isn't platform-specific) was confirmed working; the native `Alert.alert` dialog rendering was not tap-tested on-device for the same no-UI-automation reason recorded in every prior prompt.
- **Verified:** `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npx expo-doctor` (20/20 checks). On-device via the already-running `expo run:ios` dev-client session (iPhone 17, iOS 26.5), deep-linked to `cleanaudio://fine-tune/<id>` with a real recorded file (`ExpoAudio/recording-*.m4a`) as `sourceUri` — confirmed header, waveform card with real duration, AI Enhancement toggle, all four sliders at the PNG's literal values, and the added Loudness Target section render correctly and match `06-enhancement-controls.png`'s chrome. Also ran `npx expo start --web` (separate scratch port) + Playwright at a phone-sized viewport to exercise real interactions a static screenshot can't: toggling AI Enhancement off correctly enabled the sliders; dragging the Noise Removal slider updated its live percentage (80% → 90%) and made "Reset to Auto" appear; tapping it correctly restored every field (including re-enabling AI Enhancement) and hid the button again; the info sheet opened with the documented honest copy and dismissed on Escape; the leave-guard blocked a dirty header-back tap and did not block a dirty Apply tap, landing on `/processing/[jobId]?...presetId=podcast&...` with the full project param set intact. Zero console errors introduced (one pre-existing `transform-origin` RN-Web warning was confirmed to already exist on the untouched Processing screen, not introduced here).
- **Not verified:** native finger-tap/drag interaction (slider dragging, the switch, the segmented controls, the native `Alert.alert` leave-guard dialog) — this sandbox still has no `idb`/accessibility-automation path for the native simulator (same limitation recorded in every prior prompt's notes); the reachable static states were confirmed via deep-link rendering and via the web build's Playwright-driven interactions, which share the same screen/hook code but not the native gesture-recognition or native `Alert` path. VoiceOver traversal of the custom `Slider`/`AppSwitch` accessibility roles was not exercised on a real screen reader.
- **Not done in this pass (explicitly out of scope):** the real enhancement adapter these settings will eventually configure (`prompts/15-audio-domain-and-adapters.md`); persisting fine-tune settings anywhere beyond in-memory screen state (`prompts/14-zustand-and-local-data.md` — there is no store yet to save a "custom preset" into, so Reset only ever restores the Auto baseline, never a previously-saved manual preference); widening the shared `AudioProject` param contract to carry `presetId` through (see the pre-existing-gap note above).

## `prompts/10-before-after-review.md` — verification notes (2026-07-11)

- **Playback lifecycle follow-up (2026-07-11):** removed the review hook's redundant unmount-time `pause()` calls. Expo Audio's `useAudioPlayer` releases its native shared object automatically, and the additional cleanup could run after release and throw `NotFoundException` while leaving the Review screen. Background pausing and user-driven playback controls remain intact.

- **Added:** `features/audio/parseAudioProjectParams.ts` (the `AudioProject` route-param reconstruction/validation logic extracted verbatim from `useProcessingScreen.ts` into a shared module — the review screen needed the exact same reconstruction, and per `AGENTS.md` §5 the two shouldn't drift as separate copies; `useProcessingScreen.ts` now imports it instead of defining its own copy, with no behaviour change). `types/review.ts` (`ReviewFeedbackReason` — a closed enum, not free text, per this prompt's own "Collect structured reasons only"). `features/review/{enhancedAudioResult,reviewFeedbackReasons,useReviewPlayback,useReviewScreen}.ts` and `components/review/{CompareTimeline,FeedbackSheet}.tsx` — see below. Extended `lib/analytics/events.ts` with `review_feedback_submitted` (reason enum only, never audio). Added `outline` to `AppButtonVariant` (indigo border on white, for "Adjust" — none of the existing five variants matched `08-before-after.png`'s outlined pill, same precedent as `prompts/09`'s `outlineOnDark` addition) and `primary` to `AppIconButtonVariant` (solid indigo circle, for the large play button). Three new icon tokens in `constants/images.ts` (`reviewMuted`, `reviewUnmuted`, `reviewCompareHandle`), verified against the installed `Ionicons` glyph map.
- **Changed:** `src/app/review/[projectId].tsx` (real screen replacing the `prompts/03` placeholder). `src/app/_layout.tsx` — added `headerShown: false` for `review/[projectId]` (this screen now renders its own full header matching `08-before-after.png`; leaving the native stack header on produced the same double-header class of bug caught in `prompts/07`/`08`, caught here the same way before it ever reached a screenshot). `components/index.ts` (barrel exports).
- **No real enhancement adapter exists yet (`prompts/15-audio-domain-and-adapters.md` is not-started), so Enhanced playback is honestly unavailable, not fabricated (`CLAUDE.md` §8).** `features/review/enhancedAudioResult.ts#getEnhancedAudioResult()` always resolves `null` — the same always-unavailable typed-seam pattern as `presetRecommendation.ts`'s classifier stub from `prompts/08`. Concretely: Original playback is real (the project's own immutable `sourceUri`, played via `expo-audio`); selecting "Enhanced" only flips which tab is active — nothing plays, nothing is copied-and-relabelled, and the screen shows an honest "Enhanced audio isn't ready for this recording yet" caption instead. Export stays disabled with a visible explanation ("Export needs a completed enhanced version of this recording"), which is this prompt's own acceptance criterion, not a workaround. Loudness summary / peak-safety readout was omitted entirely for the same reason — the prompt's own UI list gates it on "when real metadata exists," and it doesn't yet.
- **Visual reference interpretation:** `08-before-after.png` was reproduced closely — header (back arrow, centered "Compare" title, circular icon button), segmented control, split-color waveform card with a draggable circular handle, "Swipe or drag to compare" caption, a play/progress/time row, and the Adjust/Export button pair. Two deliberate deviations, flagged per the same practice as prior prompts' PNG-vs-reality calls: (1) the segmented control reads **Original/Enhanced**, not the PNG's literal **Before/After** — this prompt's own Required UI text says "Original / Enhanced segmented control," and `CLAUDE.md`/`AGENTS.md` both rank the current numbered prompt's prose above the local PNG on exact copy. (2) the PNG's "☝️ Swipe or drag to compare" caption uses an emoji; `CLAUDE.md` §6 and `AGENTS.md` §6 both forbid emoji in production UI (no exception carved out for body-text emoji), and no other screen in this repo uses one, so it's rendered as a small `swap-horizontal-outline`-style Ionicon instead. (3) the screen defaults to the **Original** tab, not the PNG's **Enhanced/"After"** default — defaulting to a tab that's honestly unavailable in this build would show a broken-looking empty state on first load; this will match the PNG's default the moment `prompts/15` supplies a real adapter.
- **The split-color waveform reuses `WaveformPlaceholder` as-is, not a new bar renderer.** Its existing `progress` prop (already documented in `prompts/09`'s code as "for the before/after scrub comparison") colors bars left of a fraction as `waveformAfter` (indigo) and the rest as `waveformBefore` (gray) — exactly the played/unplayed split `08-before-after.png` shows. `components/review/CompareTimeline.tsx` only adds the draggable handle/line overlay on top via `PanResponder` (not `react-native-gesture-handler`'s `Gesture.Pan` — grepped the repo first and confirmed no `GestureHandlerRootView` wraps the app anywhere yet, so the built-in `PanResponder` was used instead of introducing a new gesture-handler dependency path with no existing root wiring to verify). Dragging updates a local visual-only fraction continuously (cheap, ~40 bars) and calls the real `seekTo` once on release, not on every touch-move — avoids flooding the native bridge, similar in spirit to `prompts/11`'s (not-yet-built) "debounce preview requests" instruction applied here a prompt early since the same concern applies.
- **Playback engine (`useReviewPlayback.ts`) implements the prompt's "Playback rules" against two real `expo-audio` players (Original always real; Enhanced only wired once `enhancedAudioResult` returns non-null):** never plays both at once (`otherPlayer.pause()` before any `play()`); switching tracks pauses the outgoing player and seeks the incoming one to the same clamped timestamp before resuming if it was playing; backgrounding (`AppState` → non-`"active"`) pauses both players for explicit audio-session cleanup (`enableBackgroundPlayback` is already `false` in `app.json`, so this makes the behaviour explicit rather than relying on it implicitly); a mute toggle in the header sets `.muted` on both players so it survives track switches. **Only the single-real-source path (Original) was exercised on-device** — the "switch between two real sources, seek-and-resume at the same timestamp" branch is written and code-reviewed against the same interface a real Enhanced source will use, but wasn't observable with only one genuine source available in this pass, the same class of limitation `prompts/09`'s notes recorded for its own indeterminate-progress branch.
- **A real bug was caught and fixed during on-device verification, not just visual comparison — the Processing → Review hand-off was silently broken.** `processing/[jobId].tsx`'s completion effect (`prompts/09`) previously called `router.replace({ pathname: "/review/[projectId]", params: { projectId: project?.id ?? jobId } })` — forwarding only the id, which was enough for `prompts/03`'s placeholder but leaves the real review screen with no `sourceUri` to reconstruct an `AudioProject` from, so every genuine completion hand-off landed on a "Project not found" error. Confirmed live via a deep-linked `processing/[jobId]` → mock-completion → auto-navigate run, screenshotted mid-bug. Fixed by adding `goToReview(project, { replace: true })` to `features/audio/audioProjectNavigation.ts` (same `getAudioProjectRouteParams` helper `goToPresetSelection`/`goToProcessing` already use) and calling it from the completion effect when a full `project` was reconstructed, falling back to the old id-only replace when it wasn't (a bare `jobId` deep link with no other params — same honest degradation `prompts/09`'s own `project`-optional handling already used elsewhere on that screen).
- **A second, subtler bug was caught by the same on-device run and fixed before it shipped:** changing that effect's dependency array from `project?.id` to the full `project` object made the completion effect **re-run on every render** (`useLocalSearchParams` returns a new object each render, so the `useMemo`-derived `project` is never reference-stable even though its content is), and React's cleanup-before-rerun semantics meant its own `clearTimeout` cancelled the pending navigation every single time — confirmed live as the ring sitting at a frozen "100% / Done" indefinitely while `Elapsed` kept ticking. Fixed by reverting the dependency to the stable primitive `project?.id` (matching the file's own pre-existing `elapsedSeconds`-exclusion pattern one line above it) with an inline comment and `eslint-disable-next-line react-hooks/exhaustive-deps`, then re-verified the full hand-off completes correctly.
- **Feedback ("Something sounds wrong") is a real, functioning structured picker, not a dead button** (`AGENTS.md` §20) — six closed reasons in a `BottomSheet`, matching this prompt's own list verbatim. Selecting one fires `track({ name: "review_feedback_submitted", properties: { reason } })` (a safe enum, never source audio — the prompt's own "Do not upload source audio without explicit support consent") and shows a real, visible "Thanks — noted." confirmation, honestly worded since PostHog isn't installed yet (`prompts/20`) and there is no backend to actually receive it (`prompts/16`) — same honesty convention as `prompts/09`'s notify-opt-in note.
- **Verified:** `npx tsc --noEmit` (0 errors), `npx expo lint` (0 errors/warnings), `npx expo-doctor` (20/20). On-device via the already-running `expo run:ios` dev-client session (iPhone 17, iOS 26.5), using a real recorded file left over from `prompts/07`'s on-device testing (`ExpoAudio/recording-*.m4a`, ~5.7s) as a genuine `sourceUri`: deep-linked directly to `cleanaudio://review/<id>` with the full param set `goToReview()` now produces — confirmed header/title/project-name, segmented control default state, split-color waveform with drag handle, player row (real duration `0:06` read from the actual file, not the placeholder param), "Something sounds wrong," and the disabled Export button with its explanation caption against `08-before-after.png`. Deep-linked to `cleanaudio://review/abc123` with no other params — confirmed the recoverable "Project not found" `ErrorState` renders instead of crashing. Deep-linked all the way through `cleanaudio://processing/<id>` with the same full param set and waited out the mock's real ~10s stage simulation — confirmed the fixed hand-off lands cleanly on the populated review screen (this is what surfaced both bugs above, before and after the fix).
- **Not verified:** no `idb`/accessibility-automation tooling is available in this sandbox (same limitation recorded in every prior prompt's notes, and no Playwright/browser tool was available in this session either, unlike some earlier prompts' web-based interaction passes) — so the actual finger-tap/drag interactions (segmented control tap, play/pause, dragging the playhead, the mute toggle, opening and selecting from the feedback sheet, Adjust navigation) were reviewed in code and exercised structurally via deep-linked state, but not tap-driven on a physical or simulated touch input. VoiceOver traversal of the custom `accessibilityRole="adjustable"` scrubber was not exercised on a real screen reader. Real headphone-route-change and phone-call-interruption behaviour was not device-tested (relies on `expo-audio`'s native session handling plus the explicit `AppState` backgrounding pause, not custom interruption-notification code — same honest approximation `prompts/07`'s recording-interruption notes used).
- **Not done in this pass (explicitly out of scope):** the real Enhanced audio source and real loudness/peak metadata (`prompts/15-audio-domain-and-adapters.md`); the Manual Fine-Tune screen `goAdjust()` navigates to (`prompts/11`, still the `prompts/03` placeholder — confirmed it still renders without crashing on hand-off); the Export screen `goExport()` navigates to (`prompts/12`, still a placeholder, and unreachable from this screen today since Export stays disabled); persisting feedback anywhere beyond an analytics event (`prompts/16`/`prompts/20`).

## `prompts/09-processing-screen.md` — verification notes (2026-07-11)

- **Processing review follow-up (2026-07-11):** replaced the determinate ring's clipped solid circles with an animated SVG circular stroke; completed jobs now mark every stage done; route reconstruction rejects array-valued params; notification hydration no longer overwrites an intervening local toggle; terminal analytics dedupe is keyed by job attempt; and the development adapter now enforces active-only cancellation, terminal-only retry, and includes the active stage in its remaining-time estimate. `npm run typecheck` and `npm run lint` pass. `npm run test` remains the repository's intentional failing placeholder because no test runner is installed. `expo-doctor` did not complete in this sandbox during this follow-up. Adding `react-native-svg` requires a fresh development build before native-device validation.

- **Added:** `types/processing.ts` (`ProcessingStage`, `PROCESSING_STAGE_ORDER`/`PROCESSING_STAGE_LABELS`, `ProcessingJobStatus`, `ProcessingErrorCode`, `ProcessingJobSnapshot`, `ProcessingJobAdapter` — the typed job-progress boundary `AGENTS.md` §4 requires; cancellation is modeled as three distinct statuses, `progress`/`estimatedRemainingSeconds` are `null` rather than fabricated whenever they aren't genuinely known). `services/audio/processingJobAdapter.ts` (`developmentMockProcessingAdapter` — the only `ProcessingJobAdapter` implementation today, explicitly labelled as a prototype per `AGENTS.md` §4; a module-level job registry keyed by `jobId` makes `subscribe`/`cancel`/`retry` genuinely idempotent — remounting the screen or retrying never creates a duplicate job — and it simulates real stage *transitions* over time rather than a bare timer that jumps to "done"; every snapshot is tagged `adapter: "development-mock"`). `features/processing/{useProcessingJob,useProcessingScreen,processingErrorMessages}.ts` (job subscription + real wall-clock elapsed time derived from the job's own `startedAt` so it survives remounts; `AppState` resync on foreground; route-param `AudioProject` reconstruction so a failed job can hand the same source project to preset selection; guarded one-time analytics per terminal status). `lib/notifications/jobNotificationPreference.ts` (AsyncStorage-backed "notify me when ready" opt-in — see OneSignal note below). `components/processing/{ProcessingRing,StageList}.tsx` (dependency-free circular progress — two rotating half-circle "pie" pieces masked by an inner circle, no `react-native-svg` added; indeterminate mode renders a partial spinning arc instead of a fabricated percentage; both respect `useReducedMotion`). Extended `lib/analytics/events.ts` with `processing_completed`/`processing_cancelled`/`processing_failed`/`processing_notify_opt_in_changed` (safe properties only — adapter, stage, error code, elapsed seconds, boolean).
- **Changed:** `src/app/processing/[jobId].tsx` (real screen replacing the `prompts/03` placeholder). `components/common/AppButton.tsx` — added an `outlineOnDark` variant (transparent background, translucent white border, white text) since none of the existing variants (all tuned for the light screen background) matched `07-processing.png`'s outlined pill buttons; extended via a small `resolveIconColor` helper rather than duplicating the existing color-branch logic a third time. `components/common/ErrorState.tsx` — added an `onDark` prop; see bug below. `components/index.ts` (barrel exports).
- **A real bug was caught and fixed during on-device/web verification, not just visual comparison:** `ErrorState`'s title (`AppText` with no explicit `color`, defaulting to `"primary"` = near-black ink) and its secondary button (`ghost` variant, indigo-on-transparent) are tuned for the light screen background. Screenshotting the "cancelled" and "failed" states on this screen's dark `processing` background showed the title rendering as invisible near-black-on-navy — confirmed via a Playwright screenshot before and after the fix. Fixed by adding `ErrorState`'s `onDark` prop (title/description switch to `onDark`/muted-processing-secondary colors, secondary button switches to the new `outlineOnDark` variant) and passing `onDark` from all three `ErrorState` usages on this screen (job-not-found, failed, cancelled). No other screen in the repo currently combines `ErrorState` with `background="processing"`, so this was a latent bug in the `prompts/03` placeholder that had never been visually exercised until this prompt.
- **Stage list vs. `07-processing.png`, flagged per the same practice as prior prompts' PNG-vs-prompt calls:** `CLAUDE.md` §2 / `AGENTS.md` §2 both rank the current numbered prompt above the local PNG reference. This prompt's own **Required UI** section explicitly lists 7 named stages (`preparing`, `analysing`, `removing noise`, `reducing echo`, `balancing voice`, `mastering loudness`, `finalizing`), but the PNG's screenshot shows only 6 rows with different labels/order in places (`"Balancing volume"` instead of `balancing voice`, `"Enhancing voice"` — not in the prompt's list at all — instead of `mastering loudness`, and no `preparing` row shown). The prompt's exact 7-stage set/labels were implemented as the source of truth for content; the PNG's row chrome (checkmark-circle done state, filled-dot current state, dim pending state, bold current-row text) was reproduced exactly.
- **The mock adapter's progress is a genuine derived value, never a fabricated timer-driven percentage (`CLAUDE.md` §8):** the ring's percentage is `stagesCompleted / totalStages`, a direct function of real (simulated) discrete stage transitions — not a smoothly-interpolated fake number independent of actual state. `estimatedRemainingSeconds` is likewise computed from the adapter's own known remaining stage count, never an arbitrary guess, and is `null` outside the `preparing`/`processing` statuses. The indeterminate ring (required by this prompt's own UI list) is real code — exercised as the one-frame `!snapshot` state before the job's first synchronous status arrives — but the mock's happy path always knows a stage immediately, so a real adapter's own genuine "not yet measurable" windows are what will exercise it at length; this was verified by code review, not a sustained on-screen capture.
- **"Notify me when complete" is a real, functioning opt-in, not a dead button, despite OneSignal not being installed yet** (`prompts/18-onesignal-notifications.md` is not-started). Tapping it persists a genuine, durable preference via AsyncStorage (`lib/notifications/jobNotificationPreference.ts`) and the button visibly reflects the new state (disabled, "We'll notify you when it's ready") — a real state change the user can see, satisfying `AGENTS.md` §20's "no placeholder buttons that appear functional but do nothing." It never fabricates a granted system permission, since none was ever requested (`CLAUDE.md` §11 / `AGENTS.md` §10: only request the real OS permission after a contextual accept — there is nothing to request yet). The real OS permission prompt and push delivery are wired once OneSignal lands and will read this same preference.
- **Cancellation genuinely distinguishes `cancel_requested`/`cancelling`/`cancelled`** in the typed state machine and the adapter's own transition sequence (confirmed via a Playwright interaction test: tapping Cancel immediately disables the button with a spinner, and after ~900ms the screen transitions to the distinct "Enhancement cancelled" state). The UI intentionally renders one merged "Cancelling…" treatment for the two transient sub-states rather than three visually distinct screens, since both are equally "an active cancellation in progress, not yet finished" from the user's perspective — the acceptance criterion ("never reported as an error") is satisfied by the type system itself: `cancelled` can never be coerced into `ProcessingErrorCode`, so nothing downstream (once Sentry lands in `prompts/19`) could misreport it.
- **"Reconnect after restart" is honoured only within the same JS session, not a true app-kill restart** — the mock's job registry is an in-memory `Map`, since no persisted job store exists yet (`prompts/14-zustand-and-local-data.md`). Leaving and returning to the screen (remounting) correctly reuses the same job and never creates a duplicate (verified: re-opening the same `jobId` mid-run continued from its existing stage rather than restarting); a genuine app-process restart would lose this mock's state entirely, which is an honest, documented limitation rather than a fabricated "resume" claim.
- **Verified:** `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npx expo-doctor` (20/20). On-device via the already-running `expo run:ios` dev-client session (iPhone 17, iOS 26.5): deep-linked to `cleanaudio://processing/<jobId>` with a synthetic `AudioProject`'s worth of query params (the same shape `goToProcessing()` produces) — confirmed the ring/percentage, stage checklist, waveform, elapsed/estimated time, and explanatory copy against `07-processing.png` at a live mid-run state (71%, "Mastering loudness"). Separately, re-opening a `jobId` that had already reached `completed` from an earlier deep link correctly auto-redirected to `/review/[projectId]` rather than re-showing a finished processing screen — genuine, not staged. Also ran `npx expo start --web` (separate port, scratch instance) + Playwright at a phone-sized viewport to exercise real interactions a plain screenshot can't: clicked "Notify me when complete" (confirmed it becomes disabled/re-labelled), clicked "Cancel" (confirmed the disabled spinner state, then the distinct cancelled screen ~900ms later with zero console errors throughout), and — using the same temporary-force technique documented in `prompts/06`'s notes (a jobId-prefix branch in the mock adapter forcing `status: "failed"`, screenshotted, then fully reverted and confirmed via `grep`/`git status` that no trace remained) — confirmed the failed state (title, user-safe message, "Try again"/"Choose a different preset" actions).
- **Not verified:** no `idb`/accessibility-automation tooling is available in this sandbox for native (non-web) tap-through (same limitation recorded in every prior prompt's notes) — the native on-device pass was screenshot/deep-link verification only; the interactive taps (Notify/Cancel/retry/completion redirect) were exercised on the web build instead, which shares the same screen/hook code but not the native gesture-recognition path. Real backgrounding/foregrounding on a physical timer throttle was not measured — the `AppState` resync code path was reviewed, not device-profiled. A true killed-and-relaunched app "reconnect" was not tested (see limitation above).
- **Not done in this pass (explicitly out of scope):** real native/cloud enhancement adapters and real per-stage measured progress (`prompts/15-audio-domain-and-adapters.md`); persisted job state surviving a real app restart (`prompts/14-zustand-and-local-data.md`); the real OneSignal permission request and push delivery (`prompts/18-onesignal-notifications.md`) — this pass only persists the user's opt-in intent; Sentry cancellation-not-an-error verification (`prompts/19-sentry-monitoring.md`, not installed, so nothing to verify against yet); the real Review screen this hands off to (`prompts/10-before-after-review.md` — still the `prompts/03` placeholder, confirmed it still renders without crashing on hand-off).

## `prompts/08-preset-selection.md` — verification notes (2026-07-11)

- **Added:** `types/presets.ts` (`PresetDefinition`, `PresetRoutingTarget`, `PresetRecommendationSource`, `PresetRecommendation` — the typed data model this prompt requires so DSP-adjacent metadata isn't scattered across screen files). `features/presets/presetCatalog.ts` (the 5 MVP preset definitions — stable IDs reuse the existing `PresetId` union from `types/onboarding.ts` so a preset chosen here, via a persona default, or via Record's preset shortcut all refer to the same thing; loudness targets are the actual PRD §23 platform values — -16 LUFS spoken word, -14 LUFS social — not invented numbers for presets the PRD doesn't specify one for). `features/presets/presetRecommendation.ts` (typed classifier boundary — `classifyAudioProject()` always returns `null` since no on-device classification model exists yet, `prompts/15`; falls back to the persona default read from `onboardingPreferences.ts`, then a fixed default, each tagged with an honest `source`). `features/presets/presetCopy.ts` (`getAutoCardCopy()` — maps that `source` to copy that never says "AI" unless a real classifier produced the result, this prompt's own acceptance criterion). `features/presets/usePresetSelectionScreen.ts` (route-param reconstruction of the handed-off `AudioProject`, recommendation lookup, selection state, confirm→hand-off). `components/presets/{PresetCard,PresetOptionsGrid}.tsx`. Two new icon tokens in `constants/images.ts` (`presetFieldInterview`, `presetRecommended`), reusing five already-existing tokens (`podcastMode`, `personaSocialVideos`, `personaLessonsAndTraining`, `meetingMode`, `studioEnhance`) rather than duplicating glyphs.
- **Changed:** `src/app/presets.tsx` (real screen replacing the `prompts/03` placeholder). `features/audio/audioProjectNavigation.ts` — **fixed a real bug found while wiring this screen**: `goToPresetSelection()` never forwarded `project.createdAt` or `project.presetId` in its route params, even though `record.tsx` already sets `presetId` from its preset-shortcut sheet before calling it — that selection was silently being dropped. Both fields are now forwarded, and this screen's `usePresetSelectionScreen.ts` reconstructs the full `AudioProject` from them. Also added `goToProcessing()` to the same file (documented in code as a structural hand-off only — no enhancement job exists yet, `prompts/09`/`15`). `src/app/_layout.tsx` — **fixed a second real bug**: the `presets` route was the one screen `prompts/03` left with `headerShown` not explicitly set to `false`, so the native stack header stacked above this screen's own header/back-button (the same double-header class of bug `prompts/07`'s notes recorded for `import`/`record`); caught live via an on-device screenshot, fixed by setting `headerShown: false` to match every other full-screen route in this stack.
- **Visual reference interpretation:** `cleanaudio-presets.png` is the literal layout target and was reproduced closely — header (back arrow, centered "Choose Preset" title), subtitle, 2×3 card grid (Auto Enhance + 5 presets), selected border/tint, badge pill, full-width primary CTA. `06-enhancement-controls.png` (the fine-tune sliders screen, `prompts/11`, not this one) was used only for shared chrome consistency (header/back-button style, primary-button pill shape) already captured by the existing `AppButton`/`AppIconButton` tokens — its slider/toggle UI was deliberately not built here, per this prompt's own "keep advanced controls out of this screen" instruction. Card copy (`"Social Video"`, `"Field Interview"`, `"Meeting"`) matches the PNG's literal text rather than `components/record/PresetShortcutSheet.tsx`'s pre-existing shortcut labels (`"Social Clip"`, `"Field / Interview"`, `"Call / Meeting"`) — that sheet is a distinct, already-shipped, already-verified component for a different screen (Record) and was left untouched per `CLAUDE.md`'s "preserve working behaviour outside this task"; the two ID sets are the same underlying `PresetId`s, just with a small copy variance between the compact Record shortcut and this full selection screen. Flagged here for review, same practice as prior prompts' PNG-vs-prose calls.
- **"Auto Enhance" card is not a 6th real preset.** It's a UI selection mode (`selectedId: PresetId | "auto"`) that always resolves to whatever `getPresetRecommendation()` currently returns; the 5 real presets with actual typed DSP-adjacent fields live in `presetCatalog.ts`. Its badge/subtitle honestly reflect *why* something is suggested — `"Suggested" / "Matches your usual preset"` when it's the persona default, `"Suggested" / "A good starting point"` when there's no persona at all, no badge and `"Your choice from Record"` when the user already picked a specific preset upstream (not a suggestion, so not badged as one) — verified all three states on-device (see below).
- **"Use Auto" secondary CTA** (this prompt's Behaviour spec) only renders once the user has deviated from the auto/recommended selection — `cleanaudio-presets.png`'s single screenshot shows the default state (Auto already selected, no second button), which this doesn't contradict; verified on-device that selecting a specific preset makes it appear and tapping it reverts to Auto.
- **No sample preview was built.** The prompt marks per-card audio preview as optional "if real sample assets exist" — only the unrelated onboarding demo pair (`assets/audio/demo-*.mp3`) exists, no preset-specific samples, so building a preview player here would mean either faking audio or adding a dead control (`CLAUDE.md` §8 / `AGENTS.md` §20).
- **Verified:** `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npx expo-doctor` (20/20). On-device via the already-running `expo run:ios` dev-client session (`xcrun simctl openurl`/`io screenshot`, iPhone 17, iOS 26.5): deep-linked to `cleanaudio://presets` with a synthetic `AudioProject`'s worth of query params (matching exactly what `goToPresetSelection()` now produces) — confirmed the header-fix, the full 2×3 grid, the persona-default "Suggested" auto card, and the primary CTA. Deep-linked again with a `presetId` param set (simulating a Record-shortcut carry-over) — confirmed that exact preset preselects, the Auto card correctly switches to the "not a suggestion" copy, and "Use Auto" appears. Deep-linked with no params — confirmed the recoverable `ErrorState` ("Recording not found" / "Go back") renders instead of crashing. Deep-linked to the resulting `processing/[jobId]` target with the same param shape `goToProcessing()` produces — confirmed it lands cleanly on the existing `prompts/09` placeholder with no crash and no fabricated processing claim.
- **Not verified:** actual finger-tap interaction (tapping a card, tapping "Use Auto", tapping "Enhance Audio") — this sandbox still has no `idb`/accessibility-automation path (same limitation recorded in every prior prompt's notes); state transitions were instead verified by deep-linking directly to each resulting param state, which exercises the same rendering code paths but not the `onPress` handlers' gesture recognition. The persona-default recommendation source was exercised with whatever persona was already saved in this simulator's `AsyncStorage` from earlier prompts' testing, not a controlled clean-install pass through onboarding.
- **Not done in this pass (explicitly out of scope):** the real processing screen and job creation (`prompts/09`), the real audio classifier (`prompts/15`) — `classifyAudioProject()` is a typed, always-`null` seam ready for it, manual fine-tune controls (`prompts/11`, deliberately excluded per this prompt's own instruction).

## `prompts/07-record-and-import.md` — verification notes (2026-07-11)

- **Added:** `types/audio.ts` (`AudioProject`, `AudioProjectSource`, `MediaContainer`, `MediaValidationError` typed-error class, `MediaInspectionResult` — the common project-creation model the prompt's acceptance criteria requires Record and Import to share). `services/media/{mediaFormats,mediaInspectionService,mediaImportService,mediaRecordingService}.ts` (supported-container/size rules; real duration/size/existence inspection via `expo-audio`+`expo-file-system`; copy-into-sandbox + validation pipeline for imports; `AudioProject` construction from a completed recording — both converge on the same builder shape). `features/audio/audioProjectNavigation.ts` (single `goToPresetSelection()` hand-off both flows call). `features/import/{recentImports,useImportScreen}.ts` (AsyncStorage-backed recent-imports list; picker/validation/cancel state machine). `features/record/useRecordScreen.ts` (native `expo-audio` recorder lifecycle, live metering→level-history, clipping detection, `AppState`-driven interruption handling, discard/finalize). `components/audio/{LevelMeter,RecordingWaveform}.tsx` (peak-hold meter and a real-metering-driven level history "waveform" — explicitly documented as level-over-time, not a reconstructed PCM waveform, since the recorder only exposes one dBFS reading per poll). `components/import/{ImportDropZone,ImportSourceList,RecentImportsList,ImportProgressSheet}.tsx`. `components/record/{RecordHeader,RecordControls,PresetShortcutSheet}.tsx`. New icon tokens in `constants/images.ts` (`importDropzone`, `importFromLibrary`, `importFromFiles`, `recordDot`, `recordStop`, `inputSource`), verified against the installed `Ionicons` glyph map.
- **Changed:** `src/app/import.tsx` and `src/app/record.tsx` (real screens replacing the `prompts/03` placeholders); `src/app/_layout.tsx` (`import`/`record` routes switched to `headerShown: false` since both screens now render their own in-screen header matching `05-import.png` — leaving the native stack header on produced a double-header, caught and fixed during on-device verification); `components/index.ts` (barrel exports); `app.json` (see native rebuild below); `docs/implementation-status.md`.
- **Dependencies added** (native modules — `npx expo install expo-image-picker expo-document-picker expo-file-system`): all three require a development-build rebuild, performed and verified (see below). `expo-video` was deliberately **not** added — `expo-image-picker`'s `ImagePickerAsset.duration` already supplies real video duration for Photos-sourced imports, and a Files-sourced video's duration is honestly reported as unknown (`durationSeconds: null`) rather than adding a second native module for one path; documented inline on `AudioProject.durationSeconds` and in `mediaInspectionService.ts`.
- **`app.json` config plugin fixes, not just additions:** `expo-audio`'s `recordAudioAndroid` was `false` from `prompts/00`'s scaffold (recording wasn't built yet) — left as-is it would silently omit Android's `RECORD_AUDIO` manifest permission and break recording on Android entirely. Flipped to `true` and added a custom `microphonePermission` string; added `expo-image-picker` with a custom `photosPermission` string. `expo-document-picker` needs no plugin (system file-provider UI, no Info.plist entry).
- **Visual reference interpretation:** `05-import.png` is the only screen mockup this prompt names, and it was reproduced verbatim (dashed drop card copy/icon, "Choose from Library"/"Choose from Files"/"Record Audio" rows in that exact order and icon style, the "Supports MP4, MOV, MP3, M4A, WAV, FLAC · Max 2 GB" caption). The prompt's own text additionally asks for "Choose from Photos" — the PNG instead reads "Choose from Library" and includes a third "Record Audio" row the prompt's bullet list doesn't mention; per `CLAUDE.md` §2 the local PNG outranks the prompt's own prose on exact copy, so the row is labelled and ordered exactly as shown, and its *function* (Photos library video import) still satisfies the "Photos" requirement underneath. "Recent local imports" (prompt-required, not shown in the PNG) renders only once a real entry exists, so the empty state matches the reference's blank lower half exactly — same practice as `prompts/06`'s "required sections not shown in either reference" call.
- **No local PNG shows the live Record screen** — `00-screen-overview.png`'s 12 numbered mockups skip straight from "5. Upload Screen" to "6. Enhancement Settings"; there is no recording-in-progress reference anywhere in `prompt_material/`. Composed from the already-verified design tokens/components (colors, spacing, radii, `InlineBanner`, `AppButton`) instead of approximating a look freehand, per this prompt's own working rule 3 ("use local PNGs as the required visual source" — there being none for this specific state, existing verified conventions are the next fallback per `CLAUDE.md`'s source-of-truth order).
- **Record tab/route placement:** the prompt's own Routes section suggests `app/(tabs)/record.tsx`. The already-shipped, user-confirmed tab bar (`prompts/03`) is `Library | History | Premium | Settings` with no Record tab — Record is reached from the Library screen's CTA and, now, from Import's "Record Audio" row. Adding a `(tabs)/record.tsx` would silently reintroduce a fifth tab that contradicts that confirmed decision, so the existing non-tab route (`app/record.tsx`, already scaffolded by `prompts/03`) was kept and built out in place. Flagged here for review per the same practice as `prompts/03`'s own tab-bar note.
- **Level meter and "waveform" are real, not simulated.** Both are driven by `RecorderState.metering` (dBFS) polled every 120ms from the native recorder via `useAudioRecorderState`; `RecordingWaveform` renders the actual rolling history of those samples rather than backfilling a placeholder pattern like `WaveformPlaceholder` does elsewhere in the app (`CLAUDE.md` §8 — no fabricated audio visualization). `LevelMeter`'s peak-hold decay is tick-based (holds for ~10 metering polls, ≈1.2s) rather than wall-clock-based, because a `Date.now()`-driven implementation tripped the project's `react-hooks/purity` and `react-hooks/set-state-in-effect` ESLint rules (React Compiler is enabled — `app.json`'s `reactCompiler` experiment); the tick-based version is a pure function of the `level` prop and passed lint clean.
- **Interruption/phone-call handling** is `AppState`-based: backgrounding the app while recording calls `recorder.pause()` and shows a "Recording interrupted" banner with an explicit **Resume** action (not auto-resumed, since the interruption — e.g., an incoming call — may still be ongoing). `RecorderState.mediaServicesDidReset` (iOS media-daemon reset) is also treated as an interruption. This is the same-spirit handling PRD §24 describes for recording interruption; a true iOS `AVAudioSession` interruption notification is a lower-level native signal `expo-audio` doesn't currently expose to JS, so `AppState` is the closest available proxy — documented here as an honest approximation, not a claim of full native interruption-notification coverage.
- **Discard confirmation:** `handleClose` in `record.tsx` only prompts when `hasMeaningfulRecording` (elapsed > 1s); a barely-started recording discards silently, matching the acceptance-criteria spirit of not nagging over trivial state.
- **`AudioProject` hand-off:** both flows call the same `goToPresetSelection()` (`features/audio/audioProjectNavigation.ts`), passing an identical `AudioProject` field set as route params to `/presets`. Preset selection (`prompts/08`) isn't built yet — `presets.tsx` is still the `prompts/03` placeholder — so nothing currently reads these params; this satisfies the acceptance criterion structurally (same model, same next screen) without speculatively building `prompts/08`'s own UI.
- **Verified:** `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings, including after the `react-hooks/purity`/`set-state-in-effect` fixes above), `npx expo-doctor` (20/20). Native rebuild performed (`npx expo prebuild --clean --platform ios` then `npx expo run:ios`) — **build succeeded, 0 errors** (3 pre-existing, unrelated deployment-target warnings from third-party pods, same ones noted in `prompts/05`). On-device via `xcrun simctl`: deep-linked to `cleanaudio://import` — matches `05-import.png`'s header, dashed drop card (icon/copy/"or"), three-row source list (icons, labels, chevrons), and format caption exactly, both before and after fixing a double-header bug caught live (native stack header stacked above the screen's own header until `headerShown: false` was set — see Changed above) and a safe-area bug caught live (custom header rendered under the Dynamic Island until the screen was moved onto `SafeAreaView`/`AppScreen`). Deep-linked to `cleanaudio://record`: granted microphone permission via `xcrun simctl privacy booted grant microphone`, confirming the permission-priming card (icon, copy, Enable Microphone/Not now) renders first when ungranted, and the real idle recording UI (close/elapsed "0:00"/preset-shortcut header, empty level meter, "Input: Built-in Microphone" placeholder, red "Tap to record" button) renders correctly once granted.
- **Not verified:** this sandbox has no `idb`/accessibility-automation tooling (same limitation recorded in every prior prompt's notes — `AppleScript` UI automation is blocked here too), so actually tapping the record button through to a live recording (level meter/waveform animating with real audio, pause/resume/stop, clipping detection, the interrupted-by-backgrounding banner, discard confirmation, and the Photos/Files pickers' real system UI) was not exercised on a physical input signal. The code was written and reviewed against the installed `expo-audio` v57 / `expo-image-picker` v57 / `expo-document-picker` v57 / `expo-file-system` v57 type declarations (not just docs), and the reachable static states above were confirmed rendering correctly, but a real button-press walkthrough is recommended before shipping. Android was not tested in this pass (no Android emulator running in this sandbox) — the `recordAudioAndroid: true` manifest-permission fix is a code-reviewed, not device-verified, change.
- **Not done in this pass (explicitly out of scope):** native OS share-extension import (see Known blockers above); multi-file batch import (`05-import.png` and this prompt's own UI list only show single-file selection; PRD's multi-track/batch import is P1); real audio extraction from MP4/MOV (`needsAudioExtraction: true` is set correctly on the resulting `AudioProject`, but the actual extraction adapter is `prompts/15`'s typed native/cloud boundary, not built here); preset selection's real screen (`prompts/08`) and processing (`prompts/09`) — the Record/Import preset shortcut only tags the project, it never applies any processing.

## `prompts/06-home-library.md` — verification notes (2026-07-11)

- **Added:** `types/library.ts` (`MediaType`, `ProcessingState`, `SyncState`, `ExportStatus`, `LibraryFilter`, `LibraryProject`); `features/library/{sampleLibraryData,formatDuration,formatLibraryDate,libraryFilters,useLibraryScreen}.ts` (the placeholder data source + pure filter/format helpers + the screen's state hook — loading/error/retry, search, filter, active-jobs derivation, cancel/retry/rename/duplicate/delete, all documented as placeholders pending `prompts/14`/`16`); `components/common/{BottomSheet,InlineBanner}.tsx` (new shared primitives — `BottomSheet` is PRD §27's listed "Bottom Sheet" design-system component, not built by any earlier prompt, used here for the overflow menu and rename dialog so `prompts/11`/`12` can reuse it instead of duplicating a sheet); `components/library/{LibraryHeader,PlanUsageIndicator,LibraryActions,QuickActionsRow,LibrarySearchBar,LibraryFilterPills,ActiveJobCard,ProjectListItem,ProjectOverflowSheet,RenameProjectSheet}.tsx`. 12 new icon tokens in `constants/images.ts` (`recordAction`, `mediaTypeAudio`, `mediaTypeVideo`, `cloudPlaceholder`, `syncing`, `rename`, `duplicate`, `delete`, `retry`, `cancelJob`, `openFile`), verified against the installed `Ionicons` glyph map.
- **Changed:** `src/app/(tabs)/library.tsx` (real screen replacing the `prompts/03` placeholder); `components/index.ts` (barrel exports for the new components).
- **Visual reference interpretation, flagged per the same practice used in `prompts/03`'s tab-bar decision and `prompts/04`'s demo-screen decision:** the two references cover different rows. `04-home-library.png`'s "Recent Files" rows (thumbnail, name, date, leading play button + trailing "…") are the abbreviated preview style; `10-history.png`'s rows (thumbnail, name, date, duration, trailing "…" only, plus the search bar and filter pills) are the fuller metadata style. Since the prompt's own **Project card/list item** spec explicitly requires duration and media type to be displayed — fields `10-history.png`'s rows show and `04-home-library.png`'s abbreviated rows don't — `ProjectListItem` follows `10-history.png`'s row chrome (no leading play button) for the main virtualized list, while the hero above it (greeting, headline, primary CTA, quick-action tiles) is reproduced from `04-home-library.png` verbatim. A leading play-button affordance was deliberately not added: `10-history.png` doesn't show one, and adding one would either be a dead button (no playback engine exists — `prompts/15`) or would duplicate the row's own "open" action for no functional gain (`AGENTS.md` §20 forbids buttons that appear functional but aren't backed by anything). Not surfaced to the user before implementing (Auto Mode); flagged here for review.
- **Required sections not shown in either reference, added per the prompt's own spec:** the secondary "Record" button (lighter `AppButton` under the primary "Upload Video or Audio" CTA, since the reference only shows one CTA); the extended filter set `All/Audio/Video/Processing/Enhanced` (`10-history.png` only shows `All/Audio/Video` — `Processing`/`Enhanced` were added as two more pills in the same style, per the prompt's **Required sections** list).
- **View toggle (grid vs. list) was deliberately not built**, per the prompt's own instruction ("view toggle only if both list and grid are implemented well") — only a list view exists, so no toggle is shown. Building a half-working grid view just to justify a toggle would violate `CLAUDE.md`'s "no half-finished implementations" rule.
- **Plan/usage indicator, offline banner, and storage-warning banner are built as real, independently-stylable components (`PlanUsageIndicator`, `InlineBanner`) but are not currently visible**, because their trigger conditions don't exist yet: RevenueCat (usage/plan — `prompts/17`) and a real connectivity/storage-quota signal (`prompts/16`; would need `expo-network` or `@react-native-community/netinfo`, deliberately not added speculatively without a task requirement, per `AGENTS.md` §16). `useLibraryScreen()` returns `usage: undefined`, `isOffline: false`, `storageWarning: false` today — this is an honest "not wired yet," not a fabricated always-off state, and is documented inline in the hook's return type.
- **A real bug was caught and fixed during on-device/web verification, not just visual comparison:** the first `ProjectListItem` implementation nested the trailing "…" overflow `AppIconButton` (and, for failed items, a "Retry" `Pressable`) inside the row's own `Pressable`. React Native Web renders `accessibilityRole="button"` as a real `<button>`, and a `<button>` can't legally contain another `<button>` — this surfaced as a live console error ("`<button>` cannot contain a nested `<button>`") during Playwright verification. Nested interactive elements are also an accessibility hazard on native (ambiguous VoiceOver/TalkBack touch target). Fixed by restructuring the row into three siblings — an "open" `Pressable` (thumbnail + text + badges) and a trailing `View` holding the independent "Retry"/overflow buttons — confirmed by re-running the same Playwright check with zero console errors afterward.
- **Deletion confirmation (acceptance criterion: "explains local/cloud consequences"):** `features/library/libraryFilters.ts`'s `getDeleteConsequenceMessage()` returns a different, specific message per `project.syncState` (`local_only` / `syncing` / `cloud_placeholder` / `synced`), shown via `Alert.alert` with Cancel/Delete before `deleteProject()` runs. Reviewed in code and exercised the underlying state-mutation path via the overflow sheet, but the native `Alert.alert` dialog itself couldn't be screenshotted (no UI-automation tooling in this sandbox to tap through a native alert — same limitation recorded in `prompts/03`'s notes).
- **Verified:** `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npx expo-doctor` (20/20). Screenshotted on the already-running `expo run:ios` simulator session (iPhone 17, iOS 26.5) via `xcrun simctl io screenshot`, deep-linked with `xcrun simctl openurl … cleanaudio://library`: header/CTA/quick-action tiles/Active Jobs section match `04-home-library.png`'s layout, spacing, and color language. Also verified via `npx expo start --web` + Playwright (same technique as `prompts/02`) at a phone-sized viewport, which let me exercise interactions a plain screenshot can't: typed into search ("interview" correctly narrowed to the two matching files), tapped through all 5 filter pills (each narrows the list correctly, "Enhanced" shows only `processingState: "processed"` items), opened the overflow sheet (all 5 rows render with correct icons/destructive styling for Delete), opened and cancelled the rename sheet (pre-fills the current name). Also temporarily forced the hook's `loadProjects()` to throw/return `[]` to screenshot the loading, error+retry, and first-use-empty states (reverted immediately after, confirmed via `git diff`-equivalent — file restored from a pre-edit copy — that no debug code shipped). Confirmed via the Settings tab (untouched by this prompt) that a pre-existing "Open debugger to view warnings" LogBox notice on-device is app-wide and unrelated to this change, not something introduced here.
- **Not verified:** real on-device performance with "hundreds of metadata records" (acceptance criterion) — the sample fixture has 8 entries; `FlatList` is configured with `initialNumToRender`/`windowSize`/`maxToRenderPerBatch`/`removeClippedSubviews` for virtualization, which is the correct architecture, but no actual load/scroll-performance measurement was done (would need a synthetic large fixture and an on-device profiling pass, which felt like scope creep for a prompt whose real data source is being replaced in `prompts/14` anyway). Native tap-through (as opposed to web-simulated clicks) of search/filter/overflow/rename/delete was not exercised — no `idb`/accessibility-automation available in this sandbox, matching the limitation recorded in every prior prompt's notes.
- **Not done in this pass (explicitly out of scope):** real local persistence/Zustand store (`prompts/14`), real audio adapters/thumbnails/waveform generation (`prompts/15`), real backend sync/job polling (`prompts/16`), real RevenueCat usage data (`prompts/17`), and the History tab's own screen (`prompts/13`) — `10-history.png` was used here only as a visual/structural reference for this screen's search/filter/list-row chrome, per this prompt's own **Visual references** list, not as an instruction to build the History route.

## `prompts/05-authentication-clerk.md` — verification notes (2026-07-11)

- **Added:** `@clerk/expo` (v3.7.4), `@clerk/shared` (pinned 4.25.2, matching the version `@clerk/expo` resolves — used only for `parsePublishableKey`), `expo-secure-store`, `expo-auth-session`, `expo-web-browser`, `expo-build-properties` dependencies. `lib/auth/clerk.ts` (publishable-key accessor that throws a clear error if unset, matching the official quickstart pattern; `fetchEnabledSocialStrategies()` — calls Clerk's own public `/v1/environment` endpoint, the same one Clerk's JS SDK calls on startup, to detect whether Apple/Google OAuth are genuinely enabled in the Dashboard, failing closed on any error); `lib/auth/mapClerkError.ts` (typed Clerk-error-code → user-safe-message mapping; raw provider messages are never shown). `components/auth/AuthSheet.tsx` (provider-choice screen matching `03-authentication.png`) and `SocialAuthButton.tsx`. `components/common/AppTextInput.tsx` (new design-system input primitive — no local PNG shows the email/password/code entry step, only the provider-choice step, so its look was composed from the same tokens as `AppButton`/`AppCard` rather than approximated). `src/app/(auth)/verify.tsx` (new route, shared by sign-up email verification and password reset via a `mode` param). `features/auth/{useAuthStatus,useRequireAuth}.ts` rewritten against real `@clerk/expo` `useAuth()` state (added a `"loading"` status so a cold start with a valid cached session never flashes sign-in, per the Clerk Expo skill's protected-routes guidance). `features/auth/useAuthGate.ts` (callable guard for a single guest-conversion action, e.g. a future Export button, as opposed to `useRequireAuth`'s whole-route guard). `features/auth/{useSocialSignIn,useSocialStrategyAvailability,navigation}.ts` (browser SSO via `useSSO` — never the deprecated `useOAuth` — plus the `returnTo`-preservation helpers used by all three auth screens' `finalize({ navigate })` calls). `features/auth/{identitySync,useIdentitySync}.ts` (typed RevenueCat/OneSignal/PostHog/Sentry identify-on-signin / detach-on-signout call sites, currently dev-console no-ops since those SDKs aren't installed until `prompts/17-20` — mirrors the existing `lib/analytics/events.ts` shim pattern exactly). `features/auth/{accountDeletion,useSignOutFlow}.ts` (typed handoff points, not wired to any UI yet since Settings is `prompts/21`; `requestAccountDeletion()` honestly returns a `backend_unavailable` typed error rather than fabricating success, since `prompts/16`'s backend doesn't exist). `types/auth.ts` (`SocialProvider`). 4 new icon tokens in `constants/images.ts` (`socialApple`, `socialGoogle`, `socialEmail`), verified against the installed `Ionicons` glyph map.
- **Changed:** `src/app/_layout.tsx` (mounts `ClerkProvider` + `@clerk/expo/token-cache`'s `tokenCache`, the one SDK initialized so far; added a non-rendering `AuthIdentityBridge` child to call `useIdentitySync()` from inside the provider tree; `(auth)/sign-in`, `(auth)/sign-up`, and the new `(auth)/verify` are `headerShown: false` to match `03-authentication.png`'s chrome-free full-screen look — the existing single flat `Stack` architecture was preserved, no nested `(auth)/_layout.tsx` guard was added). `src/app/(auth)/{sign-in,sign-up}.tsx` (real screens replacing the prompt-03 placeholders). `src/app/subscription.tsx`, `src/app/export/[projectId].tsx` (their existing `useRequireAuth` guard now checks `status !== "authenticated"` instead of `=== "guest"`, so the new `"loading"` status doesn't flash the placeholder content). `app.json` (added `@clerk/expo`, `expo-secure-store`, `expo-web-browser`, and `expo-build-properties` — the last configured with `ios.deploymentTarget: "17.0"`, required because Clerk's iOS SDK (`clerk-ios`, pulled in via CocoaPods' Swift Package Manager integration) requires iOS 17; the project was previously targeting 16.4). `components/index.ts` (barrel exports for the new components).
- **Apple/Google are implemented via browser SSO (`useSSO`), not native `expo-apple-authentication`/`expo-crypto` hooks**, and are only rendered when `fetchEnabledSocialStrategies()` confirms the Clerk Dashboard actually has them enabled (fail-closed default: both hidden). This satisfies the prompt's acceptance criterion ("social buttons do not appear functional unless configured") using a genuine runtime check against Clerk's own environment endpoint rather than a hardcoded flag or a guess. Interestingly, `@clerk/expo`'s config plugin auto-added the Sign in with Apple entitlement to `ios/CleanAudio/CleanAudio.entitlements` during prebuild (previously empty), so native Apple sign-in's scaffolding is already in place if a later pass wants it.
- **Combined sign-in/sign-up were kept as separate routes** (not merged into one screen), diverging from the Clerk Expo skill's general default recommendation ("one combined flow unless asked") — this prompt's own file list explicitly specifies separate `sign-up.tsx`/`sign-in.tsx` routes matching the already-existing placeholder files and root-layout `Stack.Screen` entries from `prompts/03`, and per `CLAUDE.md`'s source-of-truth order the current numbered prompt's explicit instruction outweighs a general skill default. Each screen still shares one `AuthSheet` provider-choice component and can navigate to the other.
- **MFA (`needs_second_factor`) and new-device client-trust (`needs_client_trust`) are not built.** Neither is in this prompt's explicit requirement list (email sign up/in, verification code, password reset, Apple, Google, sign out, route guards, account deletion handoff). If a Clerk instance has either enabled, `sign-in.tsx` shows a clear, honest "not supported yet — contact support" message rather than crashing or silently failing.
- **Session tasks** (forced MFA enrollment / org selection after `finalize()`) aren't built either — `features/auth/navigation.ts`'s `createAuthNavigate` still resumes `returnTo` if a session has `currentTask`, which is safe for the common case (no forced tasks configured) and is flagged in code for whichever future prompt needs task UI.
- **Security incident, resolved during this task:** partway through, `.env` changed from its start-of-session state (`EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=` present but empty) to a different state (`EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` entirely absent; `VITE_CLERK_PUBLISHABLE_KEY` and a new `CLERK_SECRET_KEY` present instead) without this session ever writing to that file. Flagged to the user live; `.env` is git-ignored so nothing was committed, and no code added in this pass ever read or logged `CLERK_SECRET_KEY` (confirmed by a repo-wide grep). The user confirmed the fix: rename `VITE_CLERK_PUBLISHABLE_KEY` → `EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY` (same real key value, just the wrong variable name for an Expo project) and delete `CLERK_SECRET_KEY` outright, since no backend in this repo needs it and secret keys must never sit in a mobile client's env. Done via a `sed` rename/delete that never printed the key values into this session's output. The source of the original `CLERK_SECRET_KEY`/`VITE_`-prefixed entry is still unconfirmed with the user — worth a follow-up.
- **Verified:** `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npx expo-doctor` (20/20). Native rebuild performed (`npx expo prebuild --clean --platform ios` — required because the previously-committed `ios/` project predated Clerk's SPM-based native dependency and its `pod install` failed until regenerated; `ios/` is not git-tracked so this was a safe, expected regeneration) followed by `npx expo run:ios`: **build succeeded, 0 errors** (3 pre-existing, unrelated deployment-target warnings from Google Sign-In's transitive pods). After the `.env` fix above, restarted Metro with a cleared cache and reloaded the dev client: **the app now boots past `ClerkProvider` into the real Library screen** (Clerk's own "loaded with development keys" notice logs, as expected for a `pk_test_` key). Deep-linked to `cleanaudio://sign-in` and `cleanaudio://sign-up` and screenshotted both (`xcrun simctl io screenshot`): `AuthSheet` matches `03-authentication.png` closely — brand mark, "Welcome to CleanAudio" headline, mode-specific subhead (confirms the `mode` prop branch), button stack order/styling, and the Terms/Privacy footer. **Both `Continue with Apple` and `Continue with Google` render** — genuine on-device confirmation that `fetchEnabledSocialStrategies()`'s runtime check against Clerk's environment endpoint correctly detected this dashboard instance has both providers enabled (not a hardcoded assumption).
- **Not verified:** no UI-automation tooling (`idb`, accessibility-permission-gated AppleScript) is available in this sandbox to simulate taps, matching the same limitation recorded in `prompts/03`/`04`'s notes — so the actual button presses through email sign-up → verification code → sign-in → forgot password → reset code → sign-out, and confirming `returnTo` resumes `/export/[id]`/`/subscription` after auth, have not been exercised on-device. The code was reviewed against the current installed `@clerk/expo` v3.7.4 type declarations (not just docs) for every method call used, but that is not a substitute for a real walkthrough — recommended before shipping.
- **Not done in this pass (explicitly out of scope):** Settings UI to actually trigger `useSignOutFlow`/`requestAccountDeletion` (`prompts/21`); real RevenueCat/OneSignal/PostHog/Sentry bodies behind `identitySync.ts` (`prompts/17-20`); the backend account-deletion endpoint itself (`prompts/16`); Terms of Service/Privacy Policy destinations for `AuthSheet`'s footer text (rendered as static styled text, not linked, since no legal-page route exists yet).

## `prompts/04-demo-onboarding-persona.md` — verification notes (2026-07-11)

- **Added:** `assets/audio/demo-original.mp3`, `assets/audio/demo-enhanced.mp3`, `assets/audio/README.md` (placeholder demo sample — see Known blockers above); `types/onboarding.ts` (`PersonaId`, `PresetId`); `features/onboarding/personas.ts` (persona catalog + persona→preset defaults), `onboardingPreferences.ts` (AsyncStorage: demo-heard, selected persona, default preset — same key-naming convention as `useOnboardingStatus.ts`'s existing `cleanaudio.onboarding.completed`), `useDemoPlayback.ts` (wraps `expo-audio`'s `useAudioPlayer`/`useAudioPlayerStatus` for the two bundled demo tracks; scoped to this one screen — the general typed audio adapters land in `prompts/15`); `lib/analytics/events.ts` (typed `AnalyticsEvent` union + `track()`, currently a dev-only console log since PostHog isn't installed until `prompts/20`); `components/audio/DemoPlaybackCard.tsx`, `components/onboarding/{SplashMark,PersonaOptionCard,StepDots}.tsx`. Added 4 icon tokens to `constants/images.ts` (`personaSocialVideos`, `personaPropertyTours`, `personaLessonsAndTraining`, `brandMark`), verified against the installed `Ionicons` glyph map.
- **Changed:** `src/app/index.tsx` (loading branch now renders `SplashMark` instead of a bare `ActivityIndicator`, no delay added), `src/app/(onboarding)/demo.tsx` and `.../persona.tsx` (replaced the prompts/03 route placeholders with real screens), `components/index.ts` (barrel exports for the new components).
- **Dependencies added** (native modules — `npx expo install expo-audio expo-asset`; `expo-asset` was a required peer dependency surfaced by `expo-doctor`): both require a development-build rebuild, which was performed and verified (see below).
- **Visual reference interpretation (flagging per the same practice used in `prompts/03`'s tab-bar decision):** only two references were named — `01-splash.png` and `02-onboarding.png` — and `02-onboarding.png` depicts a generic onboarding carousel slide (photographic mic hero, "Skip" top-right, 3-dot pagination, floating circular "next" arrow) with different headline copy than what this prompt specifies, and no audio-player affordance at all. Since the prompt's own explicit content spec (headline "Hear the difference.", bundled Original/Enhanced sample control, `Try it with my audio` / `Explore first` CTAs; persona question with 5 named options) has no functional equivalent in that single reference, the reference's *chrome* (typography scale/hierarchy, headline-then-subcopy structure, step-dot pagination, primary/secondary action pairing, spacing rhythm, indigo accent) was reproduced from centralized `constants/` tokens, while layout was adapted per-screen for the different content (an audio A/B player card instead of a static hero image on the demo screen; a single-select list of `AppCard` rows instead of a hero image on the persona screen, consistent with the design system already verified in `prompts/02`). Not surfaced to the user before implementing (Auto Mode); flagged here for review.
- **State persisted (AsyncStorage, per AGENTS.md §13):** `cleanaudio.onboarding.completed` (existing), `cleanaudio.onboarding.demoHeard`, `cleanaudio.onboarding.personaId`, `cleanaudio.onboarding.defaultPresetId`. Skipping either the demo ("Explore first") or persona ("Skip for now") always still writes a non-null default preset id (`"podcast"`), satisfying the acceptance criterion that skipping never leaves a broken default preset.
- **Analytics:** all six events from the prompt (`demo_started`, `demo_original_played`, `demo_enhanced_played`, `onboarding_cta_tapped`, `persona_selected`, `onboarding_completed`) are wired with typed, safe-only payloads (persona id / cta slug / booleans — no media, filenames, or user content). `track()` is a dev-console no-op pending `prompts/20-posthog-analytics.md`.
- **Verified:** `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npx expo-doctor` (20/20, after adding the `expo-asset` peer dependency it initially flagged). `npm run test` still only prints the placeholder message — no test runner exists yet (tracked in `prompts/22`).
- **Native rebuild performed:** ran `npx expo run:ios` against the booted "iPhone 17" (iOS 26.5) simulator to link the new `expo-audio`/`expo-asset` native modules — build succeeded (0 errors, 1 pre-existing unrelated warning) and the dev client relaunched.
- **On-device visual verification:** screenshotted the running dev client via `xcrun simctl io screenshot`, deep-linking with `xcrun simctl openurl … cleanaudio://demo` and `cleanaudio://persona` (route groups aren't part of the URL). Demo screen matches the plan: step dots, "Hear the difference." headline, supporting copy, Original/Enhanced segmented control, waveform, play button, live progress bar reading `0:00`/`0:06` (the `0:06` duration was read from the real bundled asset by `expo-audio`, confirming the native module loaded it correctly — not a hardcoded value). Persona screen matches: step dots, 5 persona rows with correct icons/labels, `Continue` correctly disabled (reduced opacity) until a selection is made, `Skip for now` present. Did **not** confirm play/pause tap-to-play behavior on-device: no `idb` and no working AppleScript UI-automation path were available in this sandbox to simulate a tap (attempted `osascript … click at`, got error -25204, likely blocked accessibility permissions) — this mirrors the same limitation `prompts/03`'s notes recorded. Also did not capture a screenshot of `SplashMark` itself: the loading window between JS-bundle-ready and `useOnboardingStatus` resolving is (correctly, per the "no artificial delay" requirement) too brief to reliably catch via polled screenshots — several attempts at varying delays landed either on the native Expo splash or already on the redirected Library screen. `SplashMark` reuses the same primitives (`WaveformPlaceholder`, `AppText`, design tokens) that did render correctly elsewhere in this pass, so it's reviewed but not directly screenshotted.
- **Not done in this pass (explicitly out of scope):** the general typed audio service interfaces/adapters (`prompts/15`), the Zustand-backed project/job store (`prompts/14`), PostHog's real dispatch (`prompts/20`), and a "Permission Priming" screen (PRD §11.1 step 5 / §15 screen list) — this prompt's own screen list only specifies Splash, demo, and persona, so it wasn't added speculatively.

## `prompts/03-navigation-and-route-shell.md` — verification notes (2026-07-11)

- **Tab bar decision:** the task prompt's illustrative route proposal and AGENTS.md §7/PRD §16 each describe a different tab set (`Library/Record/Jobs/Settings` vs `Library/Record/Real-Time Mode/Settings`), while the two visual references named by this prompt (`00-screen-overview.png`, `04-home-library.png`) — and the already-verified `constants/images.ts` from prompts/02 — show a third, already-implemented set: `Library | History | Premium | Settings`, with no dedicated Record tab (record/import reached via the Library screen's "Upload Video or Audio" CTA). Per the prompt's own instruction to "adapt names to existing conventions," this was surfaced to the user and confirmed: **matched the shipped design.** Future prompts (06, 13, 17, 21) should build against `Library`, `History`, `Premium`, `Settings`.
- Added routes under `src/app/` (the project's existing router root — confirmed via `tsconfig.json`'s `@/*` → `./src/*` alias and the pre-existing `src/app/_layout.tsx`/`index.tsx`): `(onboarding)/demo.tsx`, `(onboarding)/persona.tsx`, `(auth)/sign-in.tsx`, `(auth)/sign-up.tsx`, `(tabs)/_layout.tsx` + `library.tsx`/`history.tsx`/`premium.tsx`/`settings.tsx`, `import.tsx`, `record.tsx`, `presets.tsx`, `processing/[jobId].tsx`, `review/[projectId].tsx`, `fine-tune/[projectId].tsx`, `export/[projectId].tsx`, `file/[projectId].tsx`, `paywall.tsx` (modal presentation), `subscription.tsx`, `help.tsx`, `+not-found.tsx`. All screens are route-skeleton placeholders (no feature UI) that name the future prompt that implements them.
- Added `features/onboarding/useOnboardingStatus.ts` (AsyncStorage-backed first-launch flag, per AGENTS.md §3/§13 — approved for lightweight non-sensitive preferences, not domain data) and `features/auth/useAuthStatus.ts` + `useRequireAuth.ts` (typed guard hook, currently hardcoded to `"guest"` since Clerk isn't installed until prompts/05 — an honest default-deny rather than a faked authenticated state). `export/[projectId].tsx` and `subscription.tsx` are the two routes gated behind it, preserving the origin route as `returnTo`. Added `hooks/useRequiredParam.ts` so the four dynamic-segment screens render a recoverable not-found `ErrorState` on a missing/empty id instead of guessing.
- Dependency added: `@react-native-async-storage/async-storage` (native module, approved in AGENTS.md §3). Works today via the already-running `expo run:ios` dev-client session; a fresh `pod install`/dev-client rebuild is recommended before relying on it in a new build, since the local `ios/` prebuild predates this change.
- Config: extended `tsconfig.json` `paths` with `@/components/*`, `@/constants/*`, `@/features/*`, `@/hooks/*`, `@/lib/*`, `@/services/*`, `@/store/*`, `@/types/*` (additive only — existing `@/*` → `./src/*` and `@/assets/*` untouched) so the now-much-deeper route tree doesn't accumulate fragile `../../../` relative imports. Verified Metro resolves these the same way (screens render correctly in the simulator).
- Verified: `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npx expo-doctor` (20/20 checks). Regenerated Expo Router's typed-routes file (`.expo/types/router.d.ts`) by hitting the already-running dev server's web bundle endpoint. Visually verified against the already-running `expo run:ios` simulator session (iPhone 17, iOS 26.5): tab bar renders `Library | History | Premium | Settings` with correct icons/active-tint matching `04-home-library.png`; Library empty state renders and its "Upload Video or Audio" button navigates to `/import`; deep-linking to `/subscription` correctly redirected to `/(auth)/sign-in` with `returnTo=/subscription` preserved and visible on screen; deep-linking to an unmatched path rendered the `+not-found` screen as a real stack entry. Did not exhaustively tap through all 19 routes interactively (no `idb`/accessibility automation available in this environment) — typecheck/lint/doctor plus the above spot checks are the verification performed.
- Not done in this pass (explicitly out of scope): any screen's real feature UI (recording, import picker, preset cards, processing animation, review waveform, fine-tune sliders, export options, file detail, paywall plan comparison, sign-in/up forms) — each is built in its own numbered prompt as noted in that screen's placeholder copy.

## `prompts/02-design-system.md` — verification notes (2026-07-11)

- Added: `constants/colors.ts`, `spacing.ts`, `radii.ts`, `shadows.ts`, `typography.ts`, `theme.ts`, `images.ts`; `components/common/AppText.tsx`, `AppButton.tsx`, `AppIconButton.tsx`, `AppCard.tsx`, `AppScreen.tsx`, `StatusBadge.tsx`, `SegmentedControl.tsx`, `EmptyState.tsx`, `ErrorState.tsx`; `components/audio/WaveformPlaceholder.tsx`; `components/dev/DesignSystemShowcase.tsx`; `hooks/useReducedMotion.ts`; dev-only route `src/app/dev-design-system.tsx` (renders `null`/placeholder unless `__DEV__`).
- Dependency added: `@expo/vector-icons` (pure JS + bundled font assets via `expo-font`, already present/autolinked; no Xcode/Gradle project changes made by this task). Recommend a fresh dev-client build before relying on it on a real device, since the local `ios/` prebuild predates this change.
- Tokens were derived directly from `prompt_material/00-screen-overview.png`, `01-splash.png`, `04-home-library.png`, `06-enhancement-controls.png`, and `12-settings.png` (indigo/violet primary, deep ink text, bright neutral surfaces, gray "before" / indigo "after" waveform).
- Verified: `npm run typecheck` (0 errors), `npm run lint` (0 errors/warnings), `npx expo-doctor` (20/20). Rendered the showcase via `npx expo start --web` + Playwright screenshots at `/dev-design-system`; fixed two console warnings surfaced this way (RN-Web `shadow*` → `boxShadow` in `shadows.ts`; a Reanimated "write during render" warning in `SegmentedControl.tsx`, moved into `useEffect`). No console errors/warnings remained on final pass.
- Not done in this pass (out of scope per prompt boundaries): converting `prompts/01`'s NativeWind verification component, and wiring the showcase into real navigation (`prompts/03`).
- Native rebuild: not required for this task's JS/TS-only + font-asset changes, but a dev-client rebuild is recommended given `@expo/vector-icons`/`expo-font` and to keep the existing local `ios/` prebuild current.
